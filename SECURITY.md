# 安全策略

## 适用范围

本项目处理客户、商机、沟通记录、录音、AI 分析和微信会话数据。任何开发、测试、部署和迁移操作都必须把业务数据与源码分离。

## 禁止提交

- 真实密码、API Key、Token、Cookie 和会话密钥
- `.env` 或其他生产配置
- SSH 私钥、TLS 私钥、证书包和个人访问令牌
- SQLite 数据库、备份、WAL、SHM 和导出客户数据
- 录音、转写原文、微信会话和机器人登录状态
- 服务器快照、日志、发布压缩包和本机运行目录

示例文件只能使用明显占位值，例如 `replace-me`、`example.invalid` 或 `your-token-here`。

## 提交前检查

```bash
npm run scan:secrets
npm run test:release
git status --short
```

首次推送、迁移仓库或重写历史前，还必须扫描所有分支、标签和完整可达 Git 历史。

## 运行时要求

- 浏览器认证使用 `HttpOnly`、`Secure`、`SameSite` Cookie。
- 所有业务写入必须通过认证、CSRF、Origin、输入验证和审计。
- AI 密钥只存在于后端运行环境，不下发浏览器。
- 录音存储在发布目录之外，下载必须认证并防止路径穿越。
- 数据库备份使用在线一致性方式生成，并执行完整性、外键和哈希校验。
- 微信机器人 Token、二维码和会话目录不得写入日志或 API 响应。

## 凭据泄露处理

发现凭据进入工作区或 Git 历史时：

1. 立即停止推送和部署。
2. 在对应平台撤销并轮换凭据。
3. 清理当前文件和完整 Git 历史。
4. 重新执行全历史扫描。
5. 记录受影响范围和修复验证，不在 Issue 或 PR 中粘贴秘密值。

## 问题报告

此仓库为私有项目。安全问题应通过私下渠道报告给仓库所有者，不要创建包含凭据、客户数据或攻击细节的公开 Issue。
