# 森特智行 v0.8.0 生产数据安全

## 目标

- 每日自动数据库备份（02:30 Asia/Shanghai，在线快照、完整性校验、SHA-256、保留 14 天），解除"只在发布时备份"的空窗。
- 发布 bundle/证据归档到服务器受限目录，解除"制品仅存开发机"单点（v0.8.x 蓝图 I 阶段）。

## 主要变更

见 `CHANGELOG.md` [0.8.0]。要点：`daily-db-backup.sh`（VACUUM INTO 在线快照 + fail-closed 全链校验 + 14 天保留）、`archive-release-artifacts.sh`（不可覆盖归档 + secret 文件名拒收）、systemd `sentelligent-daily-backup.service/.timer`（219 兼容无时区写法）。无应用代码改动。

## 关键裁定（设计 `2026-08-27-v080-backup-design.md` + 实况 `2026-08-28-v080-server-facts.md`）

- TODO-10：02:30 撞发布维护窗当晚 fail-closed 跳过（cutover 自带备份兜底）。
- TODO-5 关闭（journald 实测已持久化）；TODO-6/7/8/11/12 维持后续版本决策项（失败告警接小小、异地第二副本、tools 同步入 postflight、上传哈希断言、证据深扫）。
- 安装路径：脚本装 `$BASE/tools/`（releases 之外稳定路径，单元不引用 current 铁律）；`backups/daily/`、`backups/releases/` 首建。

## 门禁（全绿）

无应用代码改动；冻结前基线复验：backend `npm test` 1275/0、根 `test:deploy` 249/0/2、`git diff --check` 干净（前端未触碰，qa:local/集成以 v0.7.7 门禁为有效基线）。

## 部署与安装证据（2026-08-28 17:13–17:23 CST，主会话亲自执行）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `b5db9bc1df2dc8e577383631764e11887648d139` / 本地注释 tag `v0.8.0` |
| bundle | SHA-256 `1411b596a2a4de10dbde168bc9c1e9001bd5e1b020381d934f659e6f8a538f13`（双端一致） |
| 服务器打包 / release | build 687 文件，归档 SHA-256 `0721292b18c0408b74def7ebf8713d7b304ea96c073d027583e53988cebe8aaf`；`releases/v0.8.0-20260828T091343Z_b5db9bc1df2d`（755/644 归一+抽查），manifest SHA-256 `f46387f51e82252f1615b547137177c8b76c91a4bd6ce871feee0a2cfe794d6e` |
| preflight | `25/25 passed`（报告 SHA-256 `ce4f4393f4d0edc8dec69c05d61ae476a9aa845a8d94c7ecbead95b15e02e511`，快照 SHA-256 `543693ee78ea3c8ec2fb2a9d07a0558be703fe0933516b1e858322c56b605699`） |
| cutover | `passed`（run `2026-08-28T09-14-36Z-29745`，零迁移）；离线备份 SHA-256 `5c2d1c066f2adac7b453c3a8c34a611ae2b908f32309cedac178cce37bb9d7b7`；微信会话备份 SHA-256 `db8ce22deb762e36207e0fd829d12f1e53cd7c2e45333183b39caffbd8b0a1cf` |
| postflight | `25/25 passed`（报告 SHA-256 `941a22ec4ebf5730e20d44bc09a81e9de8767beec250ebcced5dc937ba88639b`） |
| HTTPS 冒烟 | 首跑 `24/25`（`quick-record.preview` 模型调用瞬态降级，cleanup=clean、凭据 bytes-identical，run `3498bfdc-36e2-46de-b165-800cf4ba408c`；零应用代码改动版本，判定非回归）；重跑 `25/25 passed，cleanup=clean`（run `d2484913-271a-43df-9f34-6cf04d4d1716`，报告 SHA-256 `fc4702910abac4281b9110c9ea7346f60d444653d36c56a85c1d54763f1af3cc`） |
| **备份系统安装实测** | `tools/daily-db-backup.sh`+`archive-release-artifacts.sh`（root 0700）与 systemd 单元安装完成；`systemctl list-timers` NEXT=`Sat 2026-08-29 02:30:00 CST`；首份手动备份成功：`backups/daily/sales-workbench-daily-2026-08-28T09-21-19Z.sqlite`（1,810,432 字节，SHA-256 `cb095b342fbd104ec03994f666d4e89ace56592d8920c63d254654814a6df450`，sidecar+manifest 就位，PRUNED_COUNT=0） |
| **制品归档实测** | v0.7.0–v0.8.0 九个版本 bundle+全部 evidence 归档 `backups/releases/<version>/`（共 90MB，不可覆盖 0700/0600），九版全部 ARCHIVED |
| 回滚点 | `releases/v0.7.7-20260828T083304Z_a3b5bbc6ecc1`（未动用） |

## 边界

- 备份/归档全部 root 执行、产物 0700/0600；不动四项目服务与共享 Caddy；密钥不落盘不入日志。
