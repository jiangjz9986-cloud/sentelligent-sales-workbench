# 森特智行 v0.5.4

## 目的

v0.5.4 是 v0.5.3 的生产启动兼容热修复。现有生产环境尚未绑定真实微信 sender，因此 `WEIXIN_ALLOWED_SENDER_IDS` 为空时允许服务启动；入站边界仍拒绝所有 sender，直到运维配置真实 sender ID。

## 边界

- 不改变 v0.5.3 标签或其 GitHub Release。
- 不新增微信工具、路由或数据库迁移。
- 不把 sender、密码、令牌、Cookie、数据库内容或业务附件写入仓库或证据。
- 生产发布必须重新执行完整本地门禁、不可变包校验、25/25 预检、受控切换、HTTPS smoke 和清理检查。

## 生产结果（2026-08-16）

v0.5.4 已从 `main` 的完整提交发布并完成受控生产切换：

- 提交：`cce04a2e1f60a17be3b4491cc26dddb6b02292f9`；标签：`v0.5.4`。
- GitHub Release：[v0.5.4](https://github.com/jiangjz9986-cloud/sentelligent-sales-workbench/releases/tag/v0.5.4)。归档包含 `435` 个文件，SHA-256 为 `5e389b692907e70552b93863cd8177c9f328d28fecae70634ceb8d920e20ccfb`。
- 生产 release：`/opt/sentelligent-sales-workbench/releases/v0.5.4-20260816T050311Z_cce04a2e1f6`；回滚点：`/opt/sentelligent-sales-workbench/releases/2026-08-09_4eb6a8540ba3`。
- 切换 run：`2026-08-16T05-30-19Z-10334`，状态 `passed`，未执行回滚。切换前后 25/25 预检均通过。
- HTTPS smoke：25/25 通过，run ID `597c12ed-efac-4e3e-a409-e75936d1e598`，`cleanup=clean`。报告保存在服务器受保护 evidence 目录，不把账号、密码、Cookie 或业务正文写入仓库。
- 数据库切换前离线备份 SHA-256：`b1264d33648ffcaecbcef7785f64b6b8a63bbdccb9a0b468a89791f5cb8cbf2b`；当前库与该备份逐表一致，14 张业务/审计表合计 `71` 行，`quick_check=ok`、外键违规 `0`，本次 smoke run 无残留。
- 三个森特项目服务、共享 Caddy 和同机受保护服务均保持 `active`；Caddyfile SHA-256 保持 `68b0b38e071513f939c8a236d92d9bdc1283d2643ec88e03785ec939b0e46ba8`；HTTPS 首页和健康接口均返回 `200`。

本次生产验收覆盖发布完整性、服务切换、HTTPS 业务链路和精确清理；未绑定真实微信 sender，因此没有宣称真实微信设备往返验收。空白 sender 白名单下所有微信入站继续返回拒绝结果，待后续明确绑定真实 sender 后再做设备验收。
