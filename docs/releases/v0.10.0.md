# 森特智行 v0.10.0 快赢包

## 目标

- 落地总蓝图 v0.10.0 行（审计B Q1 六项 + §3.3 全局反馈系统）：总览今日焦点全端置顶、待办行内完成/延期+新增+remindAt、头像账号菜单、快速记录分析 pending+IME 守卫、知识清空还原+周报每日真记录、假展开七基元清除、toast+ConfirmDialog 三范式统一。
- 实施任务书：`docs/superpowers/plans/2026-08-29-v0100-quickwins-design.md`（326 行）。

## 主要变更

见 `CHANGELOG.md` [0.10.0]。两处后端（`POST /api/actions`、`PATCH remindAt`）；零数据库迁移；零新依赖。

## 审阅发现（接管工人）

- 前任七块实现基本完整；补修：`customer-opportunity-contract` 移除已删「关键联系人」断言；`qa:local` 挂接 `overview-layout`+`quickwins` 测试；集成/webkit 适配 v0.10.0（假展开静态展示、看板 toast、周报 day-toggle、头像菜单登出）。
- `window.confirm|alert` 与假展开「已展开：」在销售工作台域归零（设置页用户管理 `window.confirm` 保留，非本版范围）。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1386 通过 / 0 失败 |
| 前端 `qa:local`（CHROME_PATH） | 480 通过 / 0 失败 |
| `qa:integration`（Chrome） | passed |
| `qa:webkit` | passed |
| 根 `test:deploy` | 249 过 / 2 跳过，exit 0 |
| secret 扫描 | findings=[] |
| `git diff --check` | 干净 |
| rg 守护 | salesWorkbench 域 `window.confirm|alert|已展开：` 零命中 |

## 部署证据（2026-08-29 22:25 CST，一次通过、无回滚）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `a325b678171bf91278284cda0f36376284e76b38` / `v0.10.0` |
| bundle | `staging/sentelligent-v0.10.0-a325b67.bundle` SHA-256 `7ce2e256fa5e71526be4998ed0efe69694d5bc6985e1b6c90497ae005cf41531`（本机与服务器复算一致，600） |
| 服务器打包 | `releases/v0.10.0-20260829T141928Z_a325b67817.tar.gz` SHA-256 `fcc76117659439b11a0e97163a01ed00c68a086789bacf5f691bce3804e148be`（781 文件，commit 匹配、clean=true） |
| release 目录 | `releases/v0.10.0-20260829T141928Z_a325b67817`（VERSION=0.10.0，无新迁移） |
| 手动备份 | `backups/v0.10.0-manual-pre-cutover/manual-pre-cutover-20260829T142519Z.sqlite` SHA-256 `206a21f2cf9b82c8a67bb75ca7b8df24c61ad13babcee33a48ab7cc7bbd6ce16` |
| preflight（切换前 current=v0.9.3） | `passed`（25/25），报告 SHA-256 `dc0ec5e36864aea116680cae434e83dec1f2010438b9ed3c5dc777d1630b6e40` |
| cutover | `passed`（run `2026-08-29T14-25-20Z-27157`；首跑因缺 `.gitattributes` 失败，重打包后通过） |
| postflight | `passed`（25/25，expected-commit=a325b678…） |
| HTTPS 冒烟 | `25/25 passed`、`cleanup=clean`（run `2a2b03b6-955e-4fa5-bf9e-cf97d0922ac0`，报告 SHA-256 `6a69c90f03304c217bc79be6be2699bd0994feb573cda21eb28dad6a38409e64`；须 `cd current` 相对路径启动 smoke CLI） |
| 受保护边界 | 共享 Caddy / 轻氧店 / 招标监测器未动 |
| 回滚点 | `releases/v0.9.3-20260829T052454Z_e090d322f46a` |

## 生产验收记录（2026-08-29 22:28 CST）

- **① 总览今日焦点首屏**：生产 `VERSION=0.10.0` 已切流；`qa:integration` 桌面流断言 today-focus 先于 KPI；hero/假统计文案 rg 归零。
- **② 待办行内完成+新增+remindAt**：`POST /api/actions`+`PATCH remindAt` 后端 11 项新测全绿；HTTPS 冒烟链覆盖 customer/opportunity/action CRUD。
- **③ 头像菜单**：集成/webkit 登出改 `avatar-menu-trigger`→`avatar-menu-logout` 两击路径通过。
- **④ 快速记录 pending**：`analysisGateRef`+「分析中」门禁在 `qa:local`/`integration` 全绿。
- **⑤ 知识清空+周报真记录**：`weeklyDailyUsesRealSources` 集成断言通过；知识 onChange 空值还原单测通过。
- **⑥ 假展开消失**：七句占位 rg 归零；集成改为静态内容可见性断言通过。
- **toast/确认弹窗**：五域 `ConfirmDialog`+看板 toast；集成 customer-delete 弹窗链通过。
- **隔离回归**：`testcolleague` 账号 status=disabled，登录 403（v0.9.3 基线）；冒烟 cleanup=clean 无跨账号残留。
- **测试数据**：冒烟窗内创建实体已按脚本清理；验收待办 API 脚本因 DELETE If-Match 形态未落库（不影响冒烟 25/25）。
