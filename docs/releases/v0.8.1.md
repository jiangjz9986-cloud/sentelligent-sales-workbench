# 森特智行 v0.8.1 全站视觉统一 + PWA

## 目标

- 以差旅工作台新风格为全站基准，统一 token 与组件形态（蓝图 J 阶段）；修复视觉审查发现的 P0 无样式面板真缺陷；PWA 最小集让手机可添加到主屏。

## 主要变更

见 `CHANGELOG.md` [0.8.1]。实施依据：`docs/superpowers/research/2026-08-27-visual-audit-v081.md`（P0–P2 全部落地；P3 项见遗留）。

## 实施说明

- 子代理实施两次被平台 503 中断（17:38、17:55），主会话接管收尾：门禁复跑、冻结与部署由主会话执行；实施改动面 8 文件 +3 PWA 资产（global.css ±600 行为主体）。
- 偏差：theme-color 由 `#f6f8fb` 校准为 `#f3f5fa`（对齐全局背景实值）；语音转写误标修复采用"真实发生转写才标记"的 ref 方案（语音模式下手动输入不再误标）。

## 门禁（全绿，主会话复跑）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1275 通过 / 0 失败（本版无后端改动，基线复验） |
| 前端 `qa:local` | 418 通过 / 0 失败（净增 2 项 PWA 资产测试）；密钥扫描 findings=[] |
| `qa:integration`（Chrome）/ `qa:webkit` | passed / passed |
| 根 `test:deploy` | 249 通过 / 0 失败 / 2 平台跳过 |
| `git diff --check` | 干净 |

## 部署证据（2026-08-28 18:05–18:09 CST，主会话执行，首切一次通过）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `7fc817771d9c1dcdeb518f827276ecf721e572d3` / 本地注释 tag `v0.8.1` |
| bundle | SHA-256 `d1f7d4027597889e384f7f60dd5af27f8b6e04ad3f90ffbfb5b0335af31eb660`（双端一致） |
| 服务器打包 / release | 695 文件，归档 SHA-256 `e7deaa75c07b23cb15d358235d38615623610aa04756d0842b13f4ff4b954f3d`；`releases/v0.8.1-20260828T100520Z_7fc817771d9c`（755/644 归一），manifest SHA-256 `927550101352d4080c9f669761096e77045ab80e03cd614b80dae5d8bfc20709` |
| preflight | `25/25 passed`（报告 SHA-256 `a0d673d78b380dc33767a50278a31829dabe004c6b0ab7148a92126f17f4eecb`） |
| cutover | `passed`（run `2026-08-28T10-06-07Z-16717`，零迁移）；离线备份 SHA-256 `9637ca6b3383a351260ae1450be529b2ffb4ad583c71a5fb1887608566e2c8ba`；微信会话备份 SHA-256 `0f77e9a30c69da1c70021c7e7088952d20ba36a8e9f74cfa5d003bad85b72782` |
| postflight | `25/25 passed`（报告 SHA-256 `706c0631a6454ce865dc59689b2e12a06fa95484b9585a2b8219d312df290f90`） |
| HTTPS 冒烟 | `25/25 passed，cleanup=clean`（run `74f902ba-7593-4cb2-b2c9-b67ba8861370`，报告 SHA-256 `98ac811d1fead462245c662b4774abec4e9fe271a5cab90ff0340d0933b263d9`；凭据 bytes-identical 恢复、健康 200） |
| PWA 核验与撞路由发现 | 图标 192/512 经 HTTPS 均 200；`/manifest.webmanifest` 返回 **401**——共享 Caddy（`/etc/caddy/Caddyfile`，边界内不可动）上另一应用显式占用该根路径（另发现其 `@vaultMobileRoot` 规则会把移动 UA 的裸根路径 `/` 分流到该应用）。修复 `7c83da5`：manifest 改名 `/sentelligent.webmanifest` + `start_url=./overview` 绕开两处撞车，随 v0.8.2 上线；在此之前生产 PWA 安装暂不可用（页面功能不受影响） |
| 回滚点 | `releases/v0.8.0-20260828T091343Z_b5db9bc1df2d`（未动用） |

## 遗留（P3 未做项）

- 视觉审查 P3 低优项（个别页面次要间距/图标尺寸微调）留待日常迭代；Service Worker 离线缓存明确不做（报告建议最小集）。
