# 森特智行 v0.11.0 联动补链与模型调用纠偏

## 状态

本文件记录 `codex/v011-core-integration` 上已部署的 v0.11.0 发布结果。服务器部署、受控切换、切换后预检和 HTTPS 冒烟均已完成；桌面录音与 iPhone 真机验收按用户决定跳过，未计为通过。

| 字段 | 当前值 |
| --- | --- |
| 候选版本 | `0.11.0` |
| 分支 | `codex/v011-core-integration` |
| 基线提交 | `2dc9b1114107aee906d0ecfb006ac461cdb86cec`（v0.10.3） |
| 发布提交 | `4af911ff0d6849556759e6a024195d87d7cab24e` |
| 生产 release | `/opt/sentelligent-sales-workbench/releases/v0.11.0-20260903T111323Z_4af911f` |
| 生产状态 | 已部署；`current` 已指向上述 immutable release |
| 部署窗口 | `2026-09-03T11:18:52Z`；cutover run `2026-09-03T11-18-52Z-23826` |
| 发布就绪 | 服务器部署门通过；桌面录音和 iPhone 真机验收为 `skipped / 未执行` |

## 主要内容

- 整合招标命中转商机、拜访温度建议、发票逾期升级、统一 AI 结果卡和快速记录确认预览等 v0.11 功能。
- 作废快速记录在模型调用前拒绝，避免无效对象触发 provider。
- 统一限制 `MODEL_TIMEOUT_MS` 为 `1–120000` 毫秒，并让生产预检复用同一上限，避免 Node 定时器溢出导致近似立即超时或异常退出。
- 严格校验 `AI_ANALYSIS_MODE` 和 `MODEL_BASE_URL`；模式值会归一大小写和首尾空白，模型 URL 的首尾空白、凭据、查询串、片段和不安全协议会在配置阶段失败。
- 保留人工确认、owner 隔离、来源引用和历史只读边界。

## 模型判断

Cursor 会话日志记录了 `default` 与 `claude-fable-5` 等模型选择事件，但没有证据把这些变化归因于 Team 账号。因此本候选不把账号类型描述成模型能力下降的原因，只修复了能够复现的配置和调用缺陷。安全摘要（37 次事件：22 次 `claude-fable-5`、15 次 `default`）保存在 evidence 目录。直接周报/方案/手工建议/行程增强器在 provider 失败时仍有来源标识完善项，列入后续小任务，不把它误写成已完成。

## 门禁证据

以下结果来自候选代码线的本地运行；完整日志和版本回滚材料已归档到：
`/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-candidate-metadata-20260903/`。

| 命令 | 结果 |
| --- | --- |
| `npm run scan:secrets` | exit `0`；扫描 847 个文件、3816 个 Git 对象、727 条 Git 消息；`findings=[]` |
| `npm run test:deploy` | exit `0`；284 tests，282 pass，2 skip |
| `npm run test:release` | exit `0`；100 tests，99 pass，1 skip |
| `npm --prefix backend test` | exit `0`；1888/1888 通过 |
| `npm --prefix outputs/product-design-prototype run build` | exit `0`；Vite 1860 modules；仅有既有 manifest `theme_color` warning |
| `CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome npm --prefix outputs/product-design-prototype run qa:local` | exit `0`；684/684 通过 |
| `CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome npm --prefix outputs/product-design-prototype run qa:integration` | exit `0`；7 个视口 `overflowX=0`，冲突/鉴权/写冻结检查通过 |
| `npm --prefix outputs/product-design-prototype run qa:webkit` | exit `0`；WebKit 检查通过，`overflowX=0` |
| `TARGET_DIR=$PWD EXPECTED=0.11.0 node /Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-candidate-metadata-20260903/VERSION_CHECK.mjs` | exit `0`；8 个版本字段 `mismatchCount=0` |
| `git diff --check` | exit `0`；无输出 |

模型专项和配置专项此前已分别得到 `17/17` 与全量后端 `1888/1888`；对应独立回滚证据见：

- `/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-voided-analysis-20260903/`
- `/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-model-timeout-bound-20260903/`
- `/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-model-config-strict-20260903-1788368010/`
- `/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-candidate-metadata-20260903/`

## 生产部署证据

| 项目 | 结果 |
| --- | --- |
| Linux x64 正式制品 | `/opt/sentelligent-sales-workbench/staging/build-v0.11.0-20260903T111323Z_4af911f/sentelligent-sales-workbench-v0.11.0.tar.gz`；SHA-256 `4f3721f11590cd0b2b85616b6472f6e5e029905d4167532688576b8a59d0618b`；1005 files |
| 切换前 preflight | `25/25`；报告 `/opt/sentelligent-sales-workbench/evidence/v0.11.0-20260903T111323Z-preflight/preflight-report.json`；SHA-256 `a0708e0a428d6c2fdcd75bdd6ebb2233e1627395e133a31973155af84df98e45`；正确绑定 v0.10.3 / `2dc9b111` |
| cutover | `CUTOVER_STATUS=passed`；报告 `/opt/sentelligent-sales-workbench/evidence/v0.11.0-20260903T111323Z-cutover/2026-09-03T11-18-52Z-23826/cutover-report.json`；回滚 `not-required` |
| 切换后 postflight | `25/25`；报告 `/opt/sentelligent-sales-workbench/evidence/v0.11.0-20260903T111323Z-postflight/preflight-report.json`；SHA-256 `ddec51a2b20d1675e58f12445d8ad2079d5c74041bb5ba0d1c0cc80464f0bac7`；绑定 v0.11.0 / `4af911f` |
| HTTPS smoke | `25/25`、`failed=0`、`blocked=0`、`cleanup=clean`；run `20cbae6d-4dff-4f73-8876-9272db8d6b33`；报告 `/opt/sentelligent-sales-workbench/evidence/v0.11.0-20260903T111323Z-smoke/production-https-smoke-v0.11.0-final.json`；SHA-256 `c83137f6752e88a213705e3765623bb83d9abfd145ef708d48dd5c3f1a4ed188` |
| smoke 与 release 绑定 | `/opt/sentelligent-sales-workbench/evidence/v0.11.0-20260903T111323Z-smoke/SMOKE_SCOPE.json`；绑定 release path、commit `4af911f`、smoke run 和报告 SHA |
| 制品与证据归档 | `/opt/sentelligent-sales-workbench/backups/releases/v0.11.0/`；manifest `/opt/sentelligent-sales-workbench/backups/releases/v0.11.0/manifest.json`；`SHA256SUMS` 校验 `22/22`；归档目录 `700 root:root` |
| 运行状态 | backend、frontend、weixin-agent、Caddy 均 `active`；Caddy 与 Qingyang 未重启；`current` 指向 v0.11.0 |
| 本地四件套 | `MODIFIED_FILE`、`DIFF_FILE`、`VERIFICATION.txt`、`ROLLBACK.sh` 位于 `/Users/jiangjizhen/Documents/ChatGPT/森特智行/evidence/v011-production-deploy-20260903/`；回滚副本已验证，`MODIFIED_FILE` 保持修改态 |

### 临时凭据窗口

由于 v0.9.1 之后登录优先读取 `users.password_hash`，现有 macOS Keychain 冒烟密码与生产用户表哈希不一致。冒烟窗口只临时替换 `users.password_hash`，环境文件不改、服务不重启；窗口结束后原哈希逐字节恢复，`/dev/shm` 备份已删除，数据库 `quick_check=ok`、外键违规 `0`。原用户密码没有被永久修改。`envEqualsDb=false` 是用户表密码优先且历史改密后与 bootstrap 环境哈希不同的现有状态，不是 HTTPS 或模型故障。

## 明确跳过项

| 验收项 | 状态 | 说明 |
| --- | --- | --- |
| 桌面录音 | `skipped / 未执行` | 用户明确要求跳过；不计为通过 |
| iPhone 真机 | `skipped / 未执行` | 用户明确要求跳过；不计为通过 |

当前服务器发布结果不依赖上述两项；ASR 真实 provider 质量与性能仍作为后续专项，不在本次 smoke 结果中冒充完成。

## 后续事项

1. 本机 Keychain 条目仍保存旧的 production-smoke 密码；本次未永久改密。需要真实用户登录或下次自动化 smoke 时，按实际生产密码更新该条目。
2. ASR 真实 provider 质量与性能、以及直接周报/方案/手工建议/行程增强器的 fallback provenance，继续作为独立小任务审查，不改写本次部署结论。
3. 若候选代码再有变化，重新运行版本一致性、完整门禁、secret scan 和 `git diff --check`，再生成新的 immutable release。
