# 森特智行 v0.2.0

内容冻结时间：`2026-07-29T00:03:40.503+08:00`

## 版本目的

`v0.2.0` 把已经上线的业务系统整理为可在多台设备持续开发、可审查发布、可定位版本并可回滚的正式工程基线。运行功能延续 `f89e1e7` 的生产实现，本版重点是 GitHub 发布和长期维护资料。

## 本版内容

- 增加 `v*` 标签自动发布：Node 24 质量门、不可变源码包、manifest、SHA-256、Actions artifact 和 GitHub Release。
- 发布包秘密门禁允许 GitHub Actions 的内置上下文引用，但仍拒绝真实 token、密码和 Key。
- 统一根项目、后端和前端版本为 `0.2.0`。
- 重写 README、CHANGELOG 和路线图，记录生产地址、提交、release、服务和验收结果。
- 增加架构与模块说明，明确每个目录的责任、数据流、AI 边界和测试入口。
- 增加多设备开发手册，固定 clean clone、环境配置、worktree、PR 和设备交接流程。
- 增加发布回滚手册，固定共享 Caddy 保护、真实 release 路径和数据库恢复边界。
- 增加生产部署记录，保存不含秘密的备份、哈希、预检和冒烟证据。

## 验证要求

- 根发布测试和秘密扫描通过。
- 后端全量测试通过。
- 前端构建和 `qa:local` 通过。
- GitHub tag workflow 成功并发布可下载资产。
- 云端安装 tag 对应的 immutable release。
- 生产预检 `18/18`，公开 HTTPS 冒烟 `25/25`。
- 三个项目服务 active/enabled，共享 Caddy 和受保护服务未被重启。

## 回滚

上一已验收运行版本为 `f89e1e7`，目录 `/opt/sentelligent-sales-workbench/releases/2026-07-28_f89e1e7`。回滚时恢复该版本的三份项目 systemd 单元并重跑生产验收；数据库默认不回滚。

## 证据位置

最终 commit、tag 时间、GitHub Release `publishedAt`、归档 SHA-256 和云端部署完成时间在发布后写入 GitHub Release 和服务器 evidence。源文件不预填尚未发生的成功结果。
