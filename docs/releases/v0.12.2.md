# 森特智行 v0.12.2

## 发布身份

| 字段 | 值 |
| --- | --- |
| 版本 | `0.12.2` |
| 发布类型 | AI 统一调度平台独立底座、PushPlus 退役与微信 Clawbot 唯一外部通知通道补丁 |
| 升级基线 | `3370b9451f390cfcfa56bf8c4eb9b2df31c43a68` |
| 发布提交 | 以最终干净提交、注释 tag 和 release manifest 中的 40 位 exact commit 为准 |
| 数据库迁移 | 业务库不新增迁移；独立 AI 平台自有 `0001–0002`，当前未在业务或生产运行 |
| 生产状态 | 本轮未检查、未连接、未迁移、未切换 |

## 行为边界

- AI 统一调度平台代码已按原始精确提交整合：包含独立任务/预算/调度/管理服务、静态管理台和业务侧受限客户端。现有业务 `server.js` 未挂载该客户端，旧 AI 调用与主动 worker 未切换，生产也未安装或启动 AI 平台服务。
- PushPlus 不再是现行可用集成：没有 URL、HTTP client、token 读取、设置 mutation、测试发送、fallback 或新 `pushplus` 写入路径。
- 医院招标通知、运维告警和客户级主动助手的外部消息统一进入微信 Clawbot durable outbox，并按绑定 owner/conversation 做严格隔离。
- 没有微信绑定时，医院招标继续采集并保留审计，运维告警明确返回 `503 OPS_ALERT_DELIVERY_UNAVAILABLE`，主动助手只保留站内通知。
- 历史 `secure_settings.hospital_tender_pushplus_token` 与历史 `channel='pushplus'` 行不删除、不参与新投递，仅用于迁移兼容、审计和只读展示；现行设置接口不再暴露或修改它们。
- 微信 `context_token` 约 23 小时过期时，outbox 消息保留在 durable ledger；真实微信入站消息恢复上下文后，worker 逐条限速释放。空轮询、typing、`getconfig` 或普通 heartbeat 不作为续期保证。

## 本地验证

- AI 统一调度平台：`37/37` 通过；业务侧受限客户端：`11/11` 通过。
- 后端全量：`2036/2036` 通过。
- 根部署门禁：`286` 通过，`2` 个平台专项跳过，`0` 失败。
- 发布测试：`99` 通过，`1` 个 Windows 专项跳过，`0` 失败；Unicode 路径真实发布包构建和解压通过。
- Python 医院招标采集器：`24/24` 通过。
- 密钥扫描：工作树 `976` 个文本文件、`4095` 个完整可达 Git 对象和 `763` 条提交消息，`findings=[]`；最终计数以冻结提交后的扫描输出为准。
- Mac Google Chrome：集成 QA、滚轮专项、客户导入六视口、StageStrip 和视觉节奏 QA 通过。
- WebKit：桌面/移动视口、主动助手、写回边界和横向溢出检查通过。

## 生产与真机边界

本版本只在本地临时 SQLite、合成数据、mock AI 和 loopback 服务中验证。未读取 iCloud，未使用用户附件中的 iCloud 私钥，未连接 `82.156.210.199`，未执行生产迁移、SSH 写入、服务重启或真实微信发送。实体 iPhone 真机证据仍需设备在线、目标会话明确、发送内容和业务影响在发送前单独确认后才能记录为通过。
