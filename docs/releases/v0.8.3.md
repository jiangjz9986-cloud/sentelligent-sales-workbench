# 森特智行 v0.8.3 战情总览升级 + 行程→差旅联动

## 目标

- 落地总蓝图 K 阶段：战情总览三张新面（今日焦点卡、周趋势卡、商机漏斗增强），与晨报/周报/差旅既有口径同源；行程详情一键预填当日差旅费用骨架（URL filters 传参 + 抽屉预填 + 区域档案警示），零迁移、零新依赖、零新端点。
- 施工图：`docs/superpowers/research/2026-08-28-v082-dashboard-itinerary-design.md`（文件名保留 v082 前缀，实施版本 v0.8.3）。

## 主要变更

见 `CHANGELOG.md` [0.8.3]。后端仅扩展 `GET /api/dashboard/summary` 聚合（`backend/src/server.js` + 合同 `shared/salesWorkbenchApiContract.mjs`）；前端改动在总览（`pages.jsx`/`primitives.jsx`/`global.css`）与联动链路（`App.jsx`、`VisitItineraryPage.jsx`、新模块 `itineraryExpenseLink.js`、`TravelExpensePage.jsx`、`ExpenseEditorDrawer.jsx`、`responsibleRegionModel.js`）。

## 关键裁定与设计偏差

- **依赖漂移重核结论（设计第 14 点）**：对 v0.8.2 刚整改过的 `TravelExpensePage.jsx`/`ExpenseEditorDrawer.jsx` 以当前代码重核——`createDraft(expense, weekStart)` 签名、`regionSettingsOpen` 状态、`expense-page-alerts` 容器与 `expense-page-alert is-warning` 样式、`loadWeek`/`selectWeek` 的周失效语义全部在位，接线按语义位点落地；`TravelExpensePage` 此前不接收路由参数，本版新增 `expenseDraft`/`onExpenseDraftConsumed` 两个 prop 而非直接透传 routeFilters。视觉 token 以 v0.8.1 实际落地变量为准（`--radius-card/--radius-control/--radius-chip/--blue #2f6bff` 等），设计文中的 `#2f6fed` 色票不再出现。
- **今日焦点空态**：任务摘要"空段省略"与施工图 §2.1"每分区显示空态短文案"存在措辞差，按施工图定稿执行——四分区常驻、空分区显示"今日无行程"等引导句式（明细行省略），与 audit P1-4 空态规范一致。
- **桌面网格补位**：移除"本日推进节奏"卡后第四行只剩最近记录/重点商机两张 span-4 卡（原三卡填满 12 列），dense flow 无法凭空补 4 列，故两卡升为 span 6；今日焦点/周趋势按设计落第三行 span 6+6。移动端保持既有 KPI→hero 前缀顺序（v0.8.1 已验收现状优先），新卡插在客户温度之后、漏斗之前。
- **`rhythm` 字段去留**：合同 `dashboardSummary.rhythm` 仍为必需键、后端继续输出（微信/历史消费面零涟漪），仅 web 总览不再渲染；`overview-rhythm`/`rhythm-*` 死样式随组件删除（对齐 v0.8.2 删除 weixin-review-* 样式族的先例）。
- **待办完成数口径**：按设计风险项 1 的默认方案取 `date(substr(updated_at,1,10))` 近似完成日（complete/confirm 均重写 `updated_at`），±8h 时区边界误差接受，测试用两种历史时间格式（ISO 与 `YYYY-MM-DD HH:MM:SS`）+ 两周各自的周一/周日边界行固化。
- **今日焦点计数上限**：到点待办的 overdue/today 计数在 `LIMIT 8` 窗口内切分（展示用途，与设计 §3.3 SQL 一致），不做全表 COUNT。
- **联动预填一次性**：draft 在页面挂载时经 ref 捕获一次，URL 参数随即 `replace` 清除；prop 变 null 不影响已打开的抽屉与区域警示（区域警示随行程自然周存续，抽屉预填在关闭/保存/手工新建时清空）。
- **新招标锚点**：沿用晨报"昨日 09:00 Asia/Shanghai"锚（设计开放问题 4 默认项），tender 采集未启用时空态友好显示"近 24 小时暂无新招标"。
- **部署工具对齐金库退役现场（部署中发现的漂移）**：v0.8.2 上线后 CodexAccountVault 家族（`codex-account-vault-cloud.service`、`codex-vault-mihomo.service`、监听 4876）已由主会话按所有者授权手术退役，服务器实况核实 unit 文件已删、4876 无监听、轻氧与共享 Caddy 正常。首次 v0.8.3 preflight 的 service-plan 采集因旧受保护清单 fail-closed（预期防护），据此把三个部署脚本的受保护清单收敛为共享 Caddy + 轻氧（8797）并同步测试 fixture 与验收手册示例，重冻结后重新走全量门禁与打包。
- **总览进入即刷新（生产深测发现，中间发布 `def703d` 上验出）**：行程保存不触发 `refreshOverviewSummary`，站内切回总览时今日焦点卡不出现新行程（需整页刷新）。修复为 overview 变为活动页时静默重拉 dashboard summary，第三次冻结后重新部署验证站内导航即时可见。

## 门禁（全绿，对最终冻结提交 `a2a9eb3` 全量重跑）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1276 通过 / 0 失败（较 v0.8.2 基线 1275 净增 1 项：受控种子的 dashboard 聚合断言，含周一/周日 BETWEEN 双端点、两种 `updated_at` 历史格式、七阶段全序 + 金额 + 词表外追加、已删/已作废行排除、行程/待办/风险/招标四分区） |
| 前端 `qa:local`（CHROME_PATH 显式传入） | 434 通过 / 0 失败（较 v0.8.2 基线 422 净增 12 项：itineraryExpenseLink 双向纯函数 8、hasResponsibleCity 归一 1、travel-expense-page 联动接线合同 1、stage-strip 词表同序 1、总览进入即刷新源码断言 1）；build 成功；密钥扫描 findings=[]；EXIT=0 |
| `qa:integration`（Chrome） | passed，EXIT=0（rhythm 卡交互断言随卡移除改为今日焦点"到点待办"分区头 → 动作列表） |
| `qa:webkit` | passed，EXIT=0 |
| 根 `test:deploy` | 249 通过 / 0 失败 / 2 平台跳过 |
| `git diff --check` | 干净 |

冻结经历三轮：`1423c95`（功能全量）→ `def703d`（部署工具对齐金库退役，preflight fail-closed 触发）→ `a2a9eb3`（生产深测发现的总览进入即刷新修复）。每轮均重跑全量门禁；tag `v0.8.3` 最终指向 `a2a9eb3`。

## 部署证据（2026-08-28 21:34–22:35 CST，最终切换一次通过）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `a2a9eb3f8325296724b8ccb78841078319c5982a` / 本地注释 tag `v0.8.3`（不推 GitHub） |
| 串行门 | v0.8.2 tag 在位、工作树干净、current=v0.8.2 release、其 postflight 25/25 与冒烟 25/25（cleanup=clean）证据在位后才开工 |
| bundle | `staging/sentelligent-v0.8.3-a2a9eb3.bundle`，SHA-256 `409c00f01691199c019602dc34a49bd0976e31d0c00811f0e318125c413c030f`（本机与服务器复算一致，600 权限） |
| 服务器打包 | `staging/build-v0.8.3-20260828T142112Z_a2a9eb3f8325/`（`LC_ALL=C`，bundle detached checkout 至冻结提交、`status --porcelain` 为零，release-package status=created、manifest commit 匹配、clean=true、702 文件），归档 SHA-256 `74b72ee752f0b7732abd08d3bff8da4c9aa995dce549cf9f42cf76d3b13ea6ba` |
| release 目录（不可变冻结） | `/opt/sentelligent-sales-workbench/releases/v0.8.3-20260828T142112Z_a2a9eb3f8325`（root:root；`find -type d 755` / `-type f 644` 显式归一，根/backend/dist 抽查全 755/644、离群 0），manifest SHA-256 `c8380219ee94ccbb782c4eaff0054859a564499fc0301bf29224be08f6098aac`，VERSION=0.8.3 |
| 中间发布（保留复盘） | `releases/v0.8.3-20260828T135156Z_def703df8a76`（commit `def703d…`）曾于 21:54 CST 切换上线（preflight 25/25 `bc4155a8…`、cutover run `2026-08-28T13-54-41Z-25274` passed、postflight 25/25 `8cd4508b…`、冒烟 25/25 clean run `8357bd00-c937-4b06-9e88-3a6016356fb5` 报告 `faf6857e…`）；生产深测在其上验出总览进入不刷新缺陷后按同一 runbook 被最终发布取代，目录未删 |
| preflight（最终，对切换前 current def703d） | `25/25 passed`（expected-commit=`def703df8a76…`），报告 `…-preflight/preflight-report-20260828T142216Z.json` SHA-256 `a6c492c8a4ef249b3c2c4314ebc48d7f3946fcd65d02716edecb235b4c57396f`；pre-cutover 在线快照（node:sqlite readOnly + VACUUM INTO，quick_check=ok、外键 0）SHA-256 `ab0182a5d5f47050c8ebefdee66446458d28797b0220b63ee4abc305192224f0` |
| cutover（最终） | `passed`（run `2026-08-28T14-22-37Z-4837`，一次通过、无回滚；零迁移，`schema_migrations` 保持 27 项 last=0028，"Database migrated" 为幂等 runner 常规输出）；离线备份 SHA-256 `7e6b0b2a1e6fb666003aa6639e895c0c5f1e9e3f54d003a3bd455a2799409584`；微信会话备份 SHA-256 `d35c9e901d12b70632063a37dd5046358aa0ff2da8c7b08741a020cf35ba2661` |
| postflight | `25/25 passed`（对新 current v0.8.3 最终版，expected-commit=`a2a9eb3f8325…`），报告 `…-postflight/postflight-preflight-report-20260828T142309Z.json` SHA-256 `96c1c63773f9ef02d77a153be354bbf76653fbf94996ef5a9ed8263c0592dd13`；快照 quick_check=ok、外键 0 |
| HTTPS 冒烟（最终） | `25/25 passed`，`cleanup=clean`（一次通过，无 quick-record.preview 瞬态），run ID `f996aa25-570b-478d-ae73-871b99f236d6`，报告 SHA-256 `ec35eb0d7659a68059a39803463f05c220543b15fb04838975d8b93443a2e435`（0600）；临时凭据经 `/dev/shm` 内存备份字节级恢复（`bytes-identical`，原 env SHA-256 `bb9d15af73d5b4a504cb6b4a16ee907acc8a07235c032f29f3ee0459aa971291`），恢复后健康 200；密码经 JSON stdin 传递，仅 grep 提取 `DATABASE_URL`/`AUTH_SESSION_SECRET` 两键、全程不 source 不落日志 |
| 生产健康 | health `status=ok`、`database=ready`、`modelName=deepseek-v4-flash`、`modelReady=true`、`authEnabled=true`；HTTP→HTTPS 308；三项目服务 ExecStart 全部固定到最终 release、backend/frontend/weixin-agent/caddy 四 active；切换后 backend error 日志 0 条；线上前端资产 `index-C-4-MCLQ.js` 与 release dist 一致；PWA manifest 200（`application/manifest+json`，`start_url=./overview`）；总览三张新卡在生产真机渲染核验通过（深测截图） |
| 受保护边界 | 共享 Caddy 未动（主会话金库退役手术后的 Caddyfile 基线全程未变）；轻氧 `qingyang-store` active；CodexAccountVault 家族已退役（部署工具受保护清单本版同步收敛） |
| 回滚点 | `/opt/sentelligent-sales-workbench/releases/v0.8.2-20260828T120544Z_ef2c4f9531b8`（commit `ef2c4f9531b820c5c8237647ce61666602b36538`），未执行回滚 |

## 生产深测（用户授权测试数据，Playwright WebKit 真浏览器，全站内导航免刷新）

| 步骤 | 结果 |
| --- | --- |
| 总览三新卡渲染 | 今日焦点 4 分区、周趋势 3 指标行、商机漏斗七阶段卡 + 7 条底条（首阶段"线索"），KPI/hero/优先动作/客户温度既有卡不受影响 |
| 建今日测试行程 | `深测-联动测试行程`（visit_date=2026-08-28，济宁单站，AMAP 真实地理编码与路线规划）创建成功 |
| 今日焦点出现 | 站内切回总览即出现"深测-联动测试行程 ｜ 首站 深测测试客户甲"（进入即刷新修复生效，无需整页刷新） |
| 记当日费用 → 抽屉预填 | URL 消费后 replace 清参为 `/travel-expenses`；发生日期=2026-08-28、类目=交通、事由=`拜访 深测测试客户甲`、关联行程命中、手填客户回落"不关联"、周切至 2026-08-24—2026-08-30 |
| 保存测试费用 ¥1.00 | 账本出现该笔（含事由文本），URL 保持无参，刷新/回退不复弹抽屉 |
| 周趋势费用额变化 | ¥15.50 → ¥16.50（+¥1.00），站内导航即时更新 |
| UI 删除行程 | 列表消失、总览今日焦点即时回空态 |
| 清理零残留 | 物理删除测试费用 4 笔（含前两轮部分失败残留；付款 4 笔级联）、软删行程 6 条物理移除、相关审计 16 条移除；残留计数 expenses/payments/itineraries/audits = 0/0/0/0；本周报销额回真实基线 ¥12.50；`quick_check=ok`、外键 0 |

过程记录：首轮深测在中间发布 `def703d` 上验出"总览进入不刷新"缺陷（本版修复并重发布）；前两轮部分失败留下的同名测试数据由脚本自愈清扫 + 终局物理清理全部移除，生产真实业务数据（含本周既有 ¥12.50 报销）未受影响。
