# 森特智行 v0.9.2 数据层 + Web 硬隔离

## 目标

- 落地总蓝图 v0.9.2 行（`docs/superpowers/plans/2026-08-28-grand-plan-v09-v10.md` §4，多账号设计 L2，D1 裁定完全隔离无共享开关）：迁移 0031（owner NOT NULL 化 + ✗表补列 + 词表清扫）+ Web 全部读写按 account 硬隔离（17 域端点矩阵，差旅域为模板）+ 周报 owner 收口 + 快照适配器收紧 + 跨账号隔离矩阵测试（测试先写）。
- 实施任务书：`docs/superpowers/plans/2026-08-29-v092-isolation-design.md`（257 行实施级设计；迁移 0031 全文、17 域矩阵、写路径清单、≈96 断言测试骨架、回滚论证）。

## 主要变更

见 `CHANGELOG.md` [0.9.2]。一处数据库迁移（0031）；零新依赖；零登录/会话协议改动。

## 关键裁定与设计偏差

- **anonymous 单人开发模式语义（任务书未显式覆盖）**：`AUTH_REQUIRED=false` 时身份为 anonymous——读保持历史全库视角、写路径回落遗留池 owner（`LEGACY_OWNER='jiangjz'`，与 0031 DEFAULT 同语义）。生产恒 `AUTH_REQUIRED=true` 不受影响；该分支只为保住既有单测/本地隔离栈语义（与 `quickRecordOwnerScope` 既有先例一致）。
- **测试先写证据**：红矩阵测试单独提交（`56cbfca`，现状越权即失败——夹具本身按目标契约书写，weeklyDraft owner 必填即 422 中断），实施后 17 用例全绿。
- **serializeHospitalTenderNotice 增第三参**（`restrictMatchesToKnownCustomers`）：Web 两端点传 owner 过滤后的名映射 + 开关=true，匹配客户的 **id/理由/需求/名称四件套**只渲染映射命中者；机器同步/内部 runner/调度器路径不传开关，序列化行为零变化（比任务书"只渲染 map 命中的匹配客户"更完整——matchedCustomerIds 裸 id 也不再泄露）。
- **机器周报契约微收紧**：`weeklyDraft.owner` 降为可选后，机器**缺省** owner 时由 mismatch 判定回 403 OWNER_SCOPE_DENIED（原 schema 必填时代为 422）；mismatch 403 契约原样保留，微信侧调用方恒传 owner 不受影响。
- **修复两处微信写路径 0031 前置缺陷**：`runtimeHandlers` 的 `visit-capture.confirm` 与 `visit-capture.capture` 原先"INSERT 无 owner + 事后 UPDATE 回填"——触发器时代 INSERT 即 ABORT。改为 INSERT 即带 `context.owner`（行为等价、原子性更好）。
- **digest 集成测试账号改名**：`assistant-owner` 含连字符不合 users 账号正则（bootstrap admin 与 users CHECK 均拒绝），digest/run 加 admin 门禁后无法取得 admin 角色，harness 账号改 `assistantowner`；同因 `transaction-audit`/`quick-record-analysis` harness 账号对齐种子 owner `jiangjz`（列表/确认目标随会话硬过滤后需可见种子行）。
- **secret 扫描自查**：矩阵测试机器令牌字面量初值 `matrix-machine-token` 触发两级扫描（`matrix` 不在 placeholder 词表），改为白名单值 `test-machine-token` 并 amend 红测试提交（本会话内本地未推送提交）；冻结树两级扫描 findings=[]。
- **CRLF 行尾卫生**：编辑器在四个本已 CRLF 的 store/digest 文件中延续了 CRLF、且新建 0031 迁移混入 CR（`.gitattributes` 规定 migrations eol=lf）。本版新改/新建文件全部归一 LF（`git diff --check` 干净）；**历史遗留 CR 文件（0028 迁移/spokenDate/ops 测试组/UserManagementPage 等，本版未改动）一律不碰**，已登记总蓝图 v1.0.0-rc"行尾卫生收口"。
- **生产冒烟脚本随契约升级**：customer/opportunity 步骤 body 删 owner；weekly 步骤故意发送假 owner 并断言 `item.owner === 会话账号`（把"Web 忽略 body.owner"直接纳入生产冒烟契约）。

## weixin/adapter 夹具更新清单（语义零放宽，逐条理由）

原则：0031 回填后 owner 恒非空（触发器/NOT NULL 拒绝 NULL），全部 NULL-owner 夹具更新为"回填后形态"；防越权断言只紧不松。

| 文件 | 更新 | 理由 |
| --- | --- | --- |
| assistant-business-snapshot-adapter | 行动/风险/行程/知识夹具补显式 owner；"customer fallback"用例改名并改为 owner 列语义（历史脏交叉挂接行落第三方 owner，仍断言对 A/B 双向不可见）；knowledgeSearch 调用补 owner | NULL-owner 派生分支=已删除的死代码；不可见断言全部保留 |
| assistant-sales-loop-runtime / -preview | 行动/风险/知识/行程夹具补 owner=夹具 owner | 同上 |
| assistant-sales-loop | harness owner `继振`→`jiangjz` | 种子 owner 词表 0029/0031 后=账号 id；战情计数需与种子对齐 |
| action-item-store | "own-column/customer-join/opportunity-join"用例改为 owner 列语义（挂接行补 owner）；"write-protects legacy NULL-owner"改为"回填遗留池行对他账号读写双向关闭"（原"可见但写保护"收紧为完全不可见） | 防越权语义只紧不松 |
| opportunity-store | NULL-owner"继承商机"夹具改 owner=OWNER；直建商机/引用行补 owner | 同上 |
| quick-record-store / assistant-customer-adapter / assistant-customer-write-runtime / assistant-opportunity-http-integration / production-smoke-cleanup / api.test（dashboard 夹具）| 商机/行动/风险/快速记录直插补 owner | 触发器拒绝 NULL INSERT |
| transaction-audit / soft-delete / optimistic-lock / auth-http / validation-rules / api.test | 客户/商机请求体删 owner 字段；owner 补丁改用等价普通字段驱动回滚 | schema 收口后传 owner 即 422 |
| daily-digest-content / daily-digest-http-integration | 行程/风险夹具补 owner；harness 账号改 `assistantowner` | itinerary/risk 查询 owner 化；digest/run admin 门禁 |
| migrations.test | 行哈希对账 omit 集扩展 owner；checksum 测试客户插入补 owner；0029 用例改用 pre-0031 最小表形态 | 0031 会清扫/补列 owner；全链库触发器拒绝 NULL 夹具 |
| itinerary-migrations / sales-decision-migrations | 期望列清单追加 owner | 0031 追加列 |
| 前端 salesWorkbenchApi.test / customer-opportunity-contract.test | owner 移出可写字段与表单契约（新增反断言"负责人不得回到表单"）；draft 断言停含 owner | §3-1 写路径收口 |
| production-https-smoke.test | 周报桩返回会话账号 owner | 镜像 v0.9.2 服务端契约 |

## 端点矩阵核销（任务书 §2 逐节）

- §2.1 健康/鉴权/设置/用户管理：**保持**（复核：无业务数据读写，v0.9.1 admin 门禁原样）。
- §2.2 总览/审计/晨报：dashboard/summary 过滤 ✓（五查询+todayFocus+weeklyTrend owner 化，招标段全局）；audit-logs 保持 ✓（actor OR metadata.owner 既有过滤，矩阵断言固化）；digest/status 保持 ✓；digest/run admin 门禁+dryRun=调用者 ✓；reminders/status pendingCount 过滤 ✓；bookkeeping/client-events 保持 ✓。
- §2.3 客户域：GET 列表两分支合一 owner 过滤（机器等价不变）✓；POST 强制注入 ✓；GET/PATCH/DELETE :id 404 先于 409 ✓；微信调用点传 businessOwner ✓。
- §2.4 商机域：列表过滤 ✓；POST 注入+requireActiveCustomer(owner)→422 ✓；GET/PATCH/DELETE :id 404 ✓；diagnose-risks 两读取+draft owner ✓。
- §2.5 行动/风险域：列表过滤、PATCH/DELETE 404、深写回链 risk 补 owner、getDraftActions owner ✓。
- §2.6 知识域（D1 翻转为个人）：列表/POST/PATCH/DELETE/search + 五个注入面全部 owner 化 ✓。
- §2.7 快速记录域：现状固化 + validateCustomerOpportunityPair/confirm 目标读取恒传 account ✓。
- §2.8 AI 决策域：list/get/create owner 化、context 三实体 404、ai/suggestions 落 owner ✓。
- §2.9 周报/方案域：draftOwner=会话账号、源聚合恒带 owner、:id 端点 404、方案引用按账号校验 ✓。
- §2.10 差旅/发票/收件箱/借款/区域/工作台/记账评审：**保持**（模板域，复核人=本版实施工人，矩阵测试以机器回归+审计断言侧面覆盖，getAttachmentContent 三表贯通链未动）。
- §2.11 行程域：repository 五函数 owner 维度 + mutationFailure 前置读取带 owner ✓。
- §2.12 招标域：公告全局保留 + 名映射/匹配四件套按账号过滤 + customerId 防枚举空集 ✓；调度器全库匹配面不动（v0.9.3 #13）。
- §2.13 微信集成与机器域：`machineAuthorization` 白名单/INTEGRATION_ROUTES/outbox 租约协议零 diff ✓（git diff 无该文件）。

## 门禁（全绿，对冻结内容全量跑）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1345 通过 / 0 失败（v0.9.1 基线 1327 净增 18 用例：隔离矩阵 17 + 迁移 0031 五态 1；矩阵断言静态 100 处/运行期 ≈125 次执行，矩阵域断言 ≈110 ≥ 任务书目标 80） |
| 前端 `qa:local`（CHROME_PATH 显式传入） | 455 通过 / 0 失败（基线持平；owner 表单契约守护改向为反断言）；build 成功 |
| `qa:integration`（Chrome） | passed（历史夹具改为 0031 回填后形态：方案 owner=jiangjz、行程建后按回填语义归 jiangjz） |
| `qa:webkit` | passed |
| 根 scripts 发布测试（`test:deploy`） | 通过（251 项 249 过 2 既有跳过，exit 0；含 `project-secret-scan --history` findings=[]） |
| secret 扫描 | 工作树+git 历史 findings=[]（本版设计文档 v092 随冻结入库前自查通过；矩阵测试令牌字面量已按白名单改写） |
| `git diff --check` | 干净（本版新改文件已归一 LF） |
| 收紧完成判据 | `rg "owner IS NULL"` 四文件（businessSnapshotAdapter/actionItemStore/opportunityStore/digestContent）归零 |
| 契约红线自查 | 跨账号一律 404（矩阵断言：正确/错误 expectedVersion 同响应、无 currentVersion 字段）；全部 POST 忽略/拒绝 body owner；机器白名单与 outbox 协议零 diff；weixin 既有测试一项不减、语义零放宽 |

## 部署证据（待部署窗回填）

## 生产验收记录（待验收回填）

## 回滚论证（回代码不回数据，无害）

- ✗五表新列带 `DEFAULT 'jiangjz'`：回滚到 v0.9.1 后旧代码 INSERT 省略该列由 DEFAULT 兜底，不违反 NOT NULL，且落 jiangjz 与回滚后"无隔离单池"语义一致。
- ◇四表触发器：旧代码唯一可能写 NULL owner 的路径是 Web POST customers/opportunities 表单不带 owner（quick_records/action_items 写路径恒有 owner），命中即 500。**注意：0031 已入账本，DROP 触发器后前滚不会自动重建**——回滚 runbook 须成对执行 `DROP TRIGGER trg_<t>_owner_required_insert/_update`（×8）与配对 CREATE 原文（见迁移文件）；回滚窗口短时可用运维口径"暂不新建客户/商机"替代 DROP。
- 九枚索引与词表清扫对旧代码无害（清扫不可逆但语义=纯数据修复）。
- admin 无特权语义明示：admin 同样只见自己的业务数据（D1）；member/admin 业务面无 UI 差异。

## 遗留

- ① admin 全局审计查询面本版不做（各看各的 actor 轨迹），记 v0.9.3+。
- ② 微信仍单 owner=jiangjz：绑定表化、晨报/提醒/招标推送 per-owner 多播随 v0.9.3；招标调度器全库匹配计算面本版未动（仅堵 Web 显示面）。
- ③ owner 转移功能无（创建即定）；`?owner=me|all` 开关无（D1 裁定）。
- ④ 历史遗留 CR 行尾文件（0028 迁移/spokenDate/ops 测试组/UserManagementPage 等十余个）本版不碰，登记总蓝图 v1.0.0-rc"行尾卫生收口"。
- ⑤ env 回退轨退役路径不变（v0.9.3 评估，见 v0.9.1 遗留②）。
