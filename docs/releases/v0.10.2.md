# 森特智行 v0.10.2 移动形态+离线到达（合并版）

## 目标

- 落地总蓝图 v0.10.2 合并行：移动端底栏 4+1、下拉刷新、PWA/iOS 补全、周/日期控件降级、SW 只读壳 + bootstrap 快照、站内角标轮询。
- 实施任务书：`docs/superpowers/plans/2026-08-29-v0102-mobile-offline-design.md`。

## 主要变更

见 `CHANGELOG.md` [0.10.2]。零后端改动；零数据库迁移。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1386 通过 / 0 失败 |
| 前端 `qa:local`（CHROME_PATH） | 全绿（含新增 mobile/pwa/bootstrap-cache/service-worker 守护） |
| `qa:integration`（Chrome） | passed |
| `qa:webkit` | passed |
| 根 `test:deploy` | 249 passed / 2 skipped |
| secret 扫描 | findings=[] |
| `git diff --check` | 干净 |
| 主 chunk（build dist） | `index-*.js` ≈ **364KB**（364102 字节，gzip ≈ 107KB） |
| SW 产出 | `dist/sw.js` + workbox；`/api/` 仅 denylist/startsWith 排除，无 fetch 拦截 |

## 部署证据

（cutover 后回填 run id、SHA-256、SW curl 验收）

## 生产验收记录

（§5 九项脚本与 kill-switch 证据 cutover 后回填；tag 不动）
