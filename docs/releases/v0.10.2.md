# 森特智行 v0.10.2 移动形态+离线到达（合并版）

## 目标

- 落地总蓝图 v0.10.2 合并行：移动端底栏 4+1、下拉刷新、PWA/iOS 补全、周/日期控件降级、SW 只读壳 + bootstrap 快照、站内角标轮询。
- 实施任务书：`docs/superpowers/plans/2026-08-29-v0102-mobile-offline-design.md`。

## 主要变更

见 `CHANGELOG.md` [0.10.2]。零后端改动；零数据库迁移。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1386 通过 / 0 失败 |
| 前端 `qa:local`（CHROME_PATH） | 全绿（含新增 mobile/pwa/bootstrap-cache/service-worker 守护） |
| `qa:integration`（Chrome） | passed |
| `qa:webkit` | passed |
| 根 `test:deploy` | 249 passed / 2 skipped |
| secret 扫描 | findings=[] |
| `git diff --check` | 干净 |
| 主 chunk（build dist） | `index-*.js` ≈ **364KB**（364102 字节，gzip ≈ 107KB） |
| SW 产出 | `dist/sw.js` + workbox；`/api/` 仅 denylist/startsWith 排除，无 fetch 拦截 |

## 部署证据（2026-08-30 00:12 CST，一次通过、无回滚）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `36358e265109a50119a0d1abe50800689f02787a` / `v0.10.2` |
| bundle | `staging/sentelligent-v0.10.2-36358e2.bundle` SHA-256 `fb2964def27c562e7a5d33fd6cb99de000bacc1ba019dafab68b9a885d806112` |
| 服务器打包 | `staging/build-v0.10.2-20260829T160537Z_36358e2651/`（`LC_ALL=C`，bundle detached checkout、status 为零）；Linux 归档 SHA-256 `c0e6fabd39b2c7185693c74f2a7bb9325c40c429bd9c189e736477b764d5cec4`（本机 Darwin 归档不可用于 `release.identity`，须服务器重打） |
| release 目录 | `releases/v0.10.2-20260829T160805Z_36358e2651`（VERSION=0.10.2，零迁移） |
| 手动备份 | `backups/v0.10.2-manual-pre-cutover/manual-pre-cutover-20260829T160805Z.sqlite` SHA-256 `a4c969189c25d45d68246740e067cfe1da2be252c5379d83ea9dc777a60b4594` |
| preflight（切换前 current=v0.10.1） | `passed`（25/25），报告 SHA-256 `fce79a99bfae11f72b9d65af429bfdb6277d873a1f7e198f899db468269443a0` |
| cutover | `passed`（run `2026-08-29T16-08-06Z-797`） |
| postflight | `passed`（25/25，expected-commit=36358e26…），报告 SHA-256 `e92feaac3ee25a3028ae469221f2a9f6082ddde670b43be924333eff5d661d52` |
| HTTPS 冒烟 | 首轮 24/25（`quick-record.persisted-analysis` DeepSeek 瞬态）→ 重跑 `25/25 passed`、`cleanup=clean`（run `7cbe910f-85ff-4bbc-b8e1-4c88258af938`；报告 SHA-256 `59aad0d249fd4693ff5255d8a05f896138ff9815271482fdf356843db2c49cf4`）；临时凭据轨=users.password_hash 窗内互换、窗后 byte-exact 还原 |
| 离线 DB 备份 | `backups/v0.10.2-20260829T160805Z_36358e2651/2026-08-29T16-08-06Z-797/database-final-offline.sqlite` SHA-256 `12bd2883ca61a6c66f2c33cdbc3a6b39d7f82f84344ecb0da584a0f3d11d5326` |
| SW 首发 curl | `https://82.156.210.199/sw.js` → **200**、`Cache-Control: no-cache`；`127.0.0.1:8088/sw.js` 同源 no-cache；`/qingyang/` 仍由 Caddy 直出（200，无 SW 注册） |
| 受保护边界 | 共享 Caddy / 轻氧店未动；五服务 active |
| 回滚点 | `releases/v0.10.1-20260829T151135Z_f962ad071b` |

## 生产验收记录（2026-08-30 00:12 CST）

- **VERSION**：生产 `current` 已切至 0.10.2；三项目服务 + Caddy + 轻氧 active；`/api/health` 200。
- **SW 安全**：`sw.js` 可 200、no-cache；scope 限于工作台根；`/api/` 不拦截（denylist）；kill-switch `localStorage.sentelligent_disable_sw=1` / `sentelligent_mobile_shell=0` 为客户端回滚（§3.6）。
- **PWA/iOS meta**：`apple-mobile-web-app-*` 三行 + `sentelligent.webmanifest` 200；maskable 图标已打包。
- **桌面 ≥761px**：顶栏/横滚侧栏逻辑不变（mobile-shell 由 `useMobileShellEnabled` ≤760px 门控）；集成/webkit 门禁已绿。
- **移动端/FAB/更多/下拉/离线快照/角标/IsoWeek 降级**：自动化门禁（mobile-shell、pwa、bootstrap-cache、integration 断网场景）已绿；§5 真机九项（主屏安装、飞行模式快照、弱网、角标 60s 等）待 iPhone 截图归档至 `docs/superpowers/reports/assets/v0102-mobile-offline/`。
- **遗留**：`qa:local` 需本机 `CHROME_PATH`；vite build `theme_color` 警告（manifest 在 `public/sentelligent.webmanifest`）；真机证据目录待补 ≥9 截图 + 1 录屏。
