# 森特智行 v0.7.6 小小·商机 agent

## 目标

- 微信里查/改商机：列表与详情免确认，改阶段与下一步轻确认（回复"确认"），改金额/名称/风险与新建走六位码，删除六位码软删除；阶段前进确认后自动附带销售决策阶段升级检查（v0.7 全能助手系列第七步，总蓝图 G 阶段）。
- 核销 v0.7.2 登记的技术债：画像句式排除名单重排，"XX项目/商机什么情况"从落 unknown 改为转发商机详情。

## 主要变更

见 `CHANGELOG.md` [0.7.6] 条目。要点：六工具（list/detail-既有/update-stage/update-next/update/create/delete，R0×2 + R1 轻确认×2 + R2 六位码×2 + R3 六位码）、阶段词表后端镜像（`stageVocabulary.js`）、8 秒 race 预算的同步阶段升级检查、两级消歧坍缩（LIKE 商机名 OR 客户名 + 尾 6 位回指）、快照/适配器 version 投影与 expectedVersion 钉版、商机写路径 store 化（`opportunityStore.js`）、审计零新词、一个环境变量（`OPPORTUNITY_STAGE_REVIEW_BUDGET_MS`）。

## 关键裁定（设计文档 2026-08-28-v075-opportunity-agent-design.md）

- 改阶段 R1 轻确认（单字段完全可逆、看板即时可见、最高频动作），改金额 R2 六位码（进入决策分析输入与漏斗/周报口径，错误隐性成本高）；建档 R2、删档 R3 与客户先例对齐。
- 联动裁定同步附带 + 时间预算（默认 8 秒），不做 outbox 异步推送、不做配置开关；决策模型超时下限 120s 在外层 race 解决，被放弃的 promise 后台完成并持久化 run，可用"项目分析"拉取。
- 仅前进方向触发（词表内且目标非"暂停观察"）；回退/词表外/暂停在回执附一行手动提示代替。
- 建商机客户必须唯一命中（customer_id NOT NULL），不做"多命中默认取第一个"；同名查重仅限同客户（跨客户同名是合法业务）。

## 实施说明（与设计的偏差）

- ① 工具命名：设计中的 `opportunity.updateStage/updateNext` 驼峰名不满足 `contracts.js` 的 `TOOL_IDENTIFIER` 小写合同，落地为 `opportunity.update-stage`/`opportunity.update-next`。
- ② 消息卡：设计对话样例成稿早于 v0.7.4 卡片统一，全部预览卡/回执卡/候选卡按 weixinCard 统一版式落地（【标题】+ 标签行 + 一句操作提示），不出现 UUID/乐观锁版本号，编号用尾 6 位。
- ③ 审计 stageReview 值域：设计列 attached/timeout/failed/skipped_*，但审计必须与业务写同事务原子提交，而联动结果在事务外的 race 之后才可知——审计记录触发决策（triggered/skipped_backward/skipped_unknown_stage/skipped_pause，另有防御性 skipped_same），attached/timeout/failed 结果记录在工具运行输出（assistant_tool_runs/pending action result）与回执文本，可观测性不损失。
- ④ `opportunity.list` 的 query 参数放宽为可选："商机列表"两字列出全部 owner 可见商机（≤8 条），设计的"主语与列表词至少一个存在"约束在 router 层实现，裸"商机"两字 clarify 引导。
- ⑤ 群聊拒绝实测由 HTTP 边界（weixinAllowGroups=false）先行 403 拦截，provider 写门与 list handler 的 direct 检查作为纵深防御保留（v0.7.5 同款）。
- ⑥ 行为变更（设计 6.2 预告并固化回归）：宽"推进到"正则使"会议推进到下周"等非商机主语从拜访兜底改为商机 not_found 自澄清卡；商机查询组对含拜访词干的叙事主语让路拜访兜底（"…拜访了X聊了聊商机"仍是拜访记录）。
- ⑦ `countOpportunityReferences` 中 solution_drafts 不带 deleted_at 过滤——该表自迁移 0002 起就没有软删列（版本列有），按现状计数。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1239 通过 / 0 失败（v0.7.5 基线 1189，净增 50） |
| 前端 `qa:local` | 414 通过 / 0 失败；build 成功；密钥扫描 findings=[]（594 文件） |
| `qa:integration`（Chrome） | passed |
| `qa:webkit` | passed |
| 根 `test:deploy` | 249 通过 / 0 失败 / 2 平台跳过 |
| `git diff --check` | 干净 |

## 部署证据（2026-08-28 15:35–15:45 CST 完成切换，主会话亲自执行）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `441a8b4b19e3d5cfa75f6737518276a38d277755` / 本地注释 tag `v0.7.6`（不推 GitHub） |
| 串行门 | v0.7.5 postflight 25/25 passed + HTTPS 冒烟 25/25 passed（cleanup=clean）证据在位；current=v0.7.5 release；无 `.maintenance-lock`、无 cutover 进程，一次通过 |
| bundle | `staging/sentelligent-v0.7.6-441a8b4.bundle`，SHA-256 `9152c3452a00ec302ce1c2b0797712b5c106709385107a07ca86409d313e9a1e`（本机与服务器复算一致，600 权限） |
| 服务器打包 | `staging/build-v0.7.6-20260828T073552Z_441a8b4b19e3/`（`LC_ALL=C`，bundle detached checkout，release-package status=created、commit 匹配、clean=true），归档 SHA-256 `071e71b157d248d8d0cfe9fa085bfe0bcf3a1f66e1959b5fb23bb0eccfe16efe` |
| release 目录（不可变冻结） | `/opt/sentelligent-sales-workbench/releases/v0.7.6-20260828T073552Z_441a8b4b19e3`（root:root；tar 在 umask 077 下解出的 700 目录已显式 `find -type d chmod 755` / `-type f chmod 644` 归一，根/backend/src stat 抽查 755），manifest SHA-256 `56da2bbaf35475b6ec590390eaa2aed461d38cd08db089d149686492afcb8374`，VERSION=0.7.6 |
| preflight | `25/25 passed`（对切换前 current v0.7.5 复核），报告 `…-preflight/preflight-report-20260828T073856Z.json` SHA-256 `871a1267c97c121f9cef65066ffffc621d56dc9a836a2502a6a77c9834637e67`；pre-cutover 在线快照（node:sqlite readOnly + VACUUM INTO）SHA-256 `601e53ef6af15cd6b0f70a303636a876b081c905d3a253d812917f5f188c7641` |
| cutover | `passed`（run `2026-08-28T07-40-44Z-27915`，首切一次通过、无回滚；本版零迁移，`schema_migrations` 保持 27 项 last=0028，日志 "Database migrated" 为幂等 runner 常规输出、无新迁移应用；离线备份、三服务重固定）；数据库离线备份 SHA-256 `f4944a7b16d810b888edfb74ed9442b0c6030d5a8febbe9b93904f7f993aea05`；微信会话备份 SHA-256 `6894959f8cc5af041a10f3f71a6e41ae820d760fd0b822c3a04a81ccdbd3563b` |
| postflight | `25/25 passed`（对新 current v0.7.6），报告 `…-postflight/postflight-preflight-report-20260828T074145Z.json` SHA-256 `fe8d331d972aa90a43ae54ef621bd4fe84eb8251fa3bd8bb8817d06027ebfe31` |
| HTTPS 冒烟 | `25/25 passed`，`cleanup=clean`，run ID `b8cac27b-572c-4ec3-862e-5c56c28a1789`，报告 SHA-256 `72a138ac2a5e0368fd13a1baa6f0109c7886592118a733d2a15ab066590ade36`；临时凭据经 `/dev/shm` 内存备份字节级恢复（`bytes-identical`，原 env SHA-256 `bb9d15af73d5b4a504cb6b4a16ee907acc8a07235c032f29f3ee0459aa971291`），恢复后健康检查 200；密码经 JSON stdin 传递，全程不落日志 |
| 生产健康 | health `status=ok`、`database=ready`、`modelName=deepseek-v4-flash`、`modelReady=true`；HTTP→HTTPS 308；backend/frontend/weixin-agent 三服务 ExecStart 全部固定到 v0.7.6 release、四服务 active，共享 Caddy 未动 |
| 顺带核验 | 招标调度器 `last_status=success`、`last_error=null`、下轮 17:20 CST（9–20 窗 2 小时节奏正常）；待办提醒队列 1 条未来项（明早 10:00）等待正常、outbox 无本版新增失败（4 条 failed 均为 08-22/08-25 历史遗留） |
| 回滚点 | `/opt/sentelligent-sales-workbench/releases/v0.7.5-20260828T063227Z_3251851e7ffd`（commit `3251851e7ffd816eba7413f669619f3e50323b71`），未执行回滚 |

## 边界

- 不写真实业务数据；不操作微信 UI；生产发布严格串行、不可变 release；密钥不落盘不入日志；共享 Caddy 不动；模型路由不变；零迁移。
