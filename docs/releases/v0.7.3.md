# 森特智行 v0.7.3 小小·拜访与快速记录 agent

## 目标

- 把拜访/电话/会议记录收进微信助手"小小"：`记一下：…` 一句话记录（轻确认），查历史免确认，改历史与作废走六位码（v0.7 全能助手系列第四步，总蓝图 E 阶段）。
- 补齐 `voided_at` 自迁移 0002 建列以来"有列无路"的作废能力缺口。

## 主要变更

- 四个快速记录工具注册进确定性编排器：`visit-capture.capture`（R1/affirm_language 轻确认）、`visit-capture.search`（R0 免确认）、`visit-capture.update`（R2/explicit_code）、`visit-capture.void`（R3/explicit_code）。写操作仅限与小小绑定的私聊，owner 由服务端解析。
- `记一下/记录一下/帮我记/快速记录/记拜访：…` 一步式捕获：同步 AI 分析（30s 超时静默降级确定性 fallback）→ 摘要卡（要点/客户匹配/待办建议）→ 回复"确认"写回。写回复用 quick_record 确认深写回链路（客户/商机/待办/风险联动），`source_channel='微信助手'` 直通周报素材路径不变。
- R1 轻确认复用记账"确认"交互的内部派生凭据模式，`pendingActionRepository` 零改动；编排器新增 `affirm_language` 确认分支，与六位码链共享 TTL/取消/重发语义。
- 与记账的歧义分流：金额（`N 元/块`）与记账词（记账/支出/收入/借款/报销/发票/付款）强信号让路记账链路；`记拜访：` 为绕开歧义的逃生门；拜访信号词（拜访/电话/会议/客户/医院/项目等）加权归属快速记录。
- 检索：`查（一下）…（今天/昨天/上周/本周/上月/本月/最近…）…的（拜访）记录`，中文口语时间窗解析（新增 `backend/src/assistant/spokenDate.js`，过去向），客户主语复用 v0.7.2 消歧器；候选卡带记录短码。
- 改历史：`把（那条/记录<短码>）的<字段>改成…`，字段白名单（发生时间/客户/商机/诉求/反馈/风险/建议/下一步），R2 预览卡 before/after + 乐观锁 `expectedVersion` 钉版。
- 作废：`作废/删除/撤销（那条）记录（<短码>）`，R3 六位码；首次为 `voided_at` 打通写路径，软作废可审计、读路径自动隐藏、重复作废冲突保留首次证据。
- 写路径抽取共享模块 `backend/src/quickRecords/quickRecordStore.js`（Web 与微信同一份 SQL/审计/版本冲突语义）；新增 `backend/src/assistant/quickRecordPendingPreviewProviders.js` 预览提供者。既有微信三步式 visit-capture 暂存流原样保留。
- 记账草稿并存回归合同：T-BK-1/2/3 在单元（shortcut-bookkeeping-safety）与 HTTP 集成两层固化——affirmed capture 无引用时 handlePending 让路、null capture+记账草稿并存让路、quoted 记账草稿优先（留在记账运行时应答 409"引用草稿非当前可确认版本"，真实入账由 HTTP 集成覆盖）。
- 零数据库迁移；模型路由不变（文本 deepseek-v4-flash；图片/PDF deepseek-v4-flash-vision-exp）。

## 实施说明

- 本版由主会话在子代理两次中断后接管收尾：实施与测试主体来自子代理（HTTP 集成 10/10、单元合同用例已落盘），主会话复核现场、补跑全量门禁、冻结与部署。
- 设计文档：`docs/superpowers/research/2026-08-28-v073-quickrecord-agent-design.md`；quoted 单元用例按 harness 假 DB 实际行为断言 409（与 v0.7.2 偏差 3 同理）。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1145 通过 / 0 失败（v0.7.2 基线 1072，净增 73） |
| 前端 `qa:local` | 414 通过 / 0 失败；build 成功；密钥扫描 findings=[]（570 文件） |
| `qa:integration`（Chrome） | passed |
| `qa:webkit` | passed |
| 根 `test:deploy` | 249 通过 / 0 失败 / 2 平台跳过 |
| `git diff --check` | 干净 |

## 部署证据（2026-08-28 09:09–09:24 CST 完成切换，主会话亲自执行）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `9f8a11efaf2e00156b9a8653399e8e8e8383d734` / 本地注释 tag `v0.7.3`（不推 GitHub） |
| bundle | `staging/sentelligent-v0.7.3-9f8a11e.bundle`，SHA-256 `656a4083dec82c85c01fc8c566841543245720427ce7fbbc942a9673973f2dc3`（本机与服务器复算一致） |
| 服务器打包 | `staging/build-v0.7.3-20260828T010944Z_9f8a11efaf2e/`（bundle detached checkout + release-package.mjs，source clean、node v24.18.0 / npm 11.16.0），归档 653 文件，SHA-256 `d61348a8466cc130407750bc9d3863af1d4b9dc3ef538043ce645ff58bcf1eb8` |
| release 目录（不可变冻结） | `/opt/sentelligent-sales-workbench/releases/v0.7.3-20260828T010944Z_9f8a11efaf2e`（root:root、go-w 全清），manifest SHA-256 `a64b776731ff062c740009881f2307e081e070511e1c71daba625087e9fa6f30` |
| preflight | `25/25 passed`（对切换前 current v0.7.2 复核），报告 `evidence/v0.7.3-…-preflight/preflight-report.json`（SHA-256 `a9b8f0b64f1ca0a214679b7c7435e9a24abb537644dca917ad0ce24e029edc84`），pre-cutover 在线快照 SHA-256 `64158d4d1debfc0ac66cfc341e6bbd9ccc2144e6b681349d14abd45346cbd6d5` |
| cutover | `passed`（run `2026-08-28T01-12-51Z-3783`，无迁移、离线备份、三服务重固定）；数据库离线备份 SHA-256 `15ec0429960cd5d20228dde75ae44c39ac46ae600fe61a66d495253028bf587f`；微信会话备份 SHA-256 `c580d692dc2b1b0df45ba08b65272180387e7873d018f8255ac7e73b5deb9fc8` |
| postflight | `25/25 passed`（对新 current v0.7.3），报告 `evidence/v0.7.3-…-postflight/postflight-preflight-report.json` |
| HTTPS 冒烟 | `25/25 passed`，`cleanup=clean`（audit manifest 12 条、session 登出、服务器数据清理全过），run ID `5a64fb36-9cb0-4a9a-a8e7-34d0579ac69a`；临时凭据经 `/dev/shm` 内存备份字节级恢复（前后 env SHA-256 一致 `bytes-identical`），恢复后健康检查 200 |
| 生产健康 | `https://82.156.210.199/api/health` 返回 `status=ok`、`database=ready`、`modelName=deepseek-v4-flash`、`modelReady=true`、`authEnabled=true`；HTTP→HTTPS 308；backend/frontend/weixin-agent 三服务 ExecStart 全部固定到 v0.7.3 release、四服务 active，共享 Caddy 未动 |
| 迁移面 | 零迁移版本：`schema_migrations` 保持末项 `0027`（26 项），符合设计 |
| 回滚点 | `/opt/sentelligent-sales-workbench/releases/v0.7.2-20260827T172222Z_50b8be8860d2`（commit `50b8be8860d2d7ccf3234c6da1ac7b2e79c471e6`），未执行回滚 |

## 边界

- 不写真实业务数据；不操作微信 UI；生产发布严格串行、不可变 release；密钥不落盘不入日志；共享 Caddy 不动。
