# 森特智行 v0.7.7 每日晨报 + 周五收尾包（v0.7 系列收官）

## 目标

- 每日 09:00（Asia/Shanghai）小小微信晨报：今日行程 + 待办（逾期/今日/未排期）+ 活跃风险 + 昨日以来新招标，首行确定性"焦点"；周五 16:30 收尾包：周报草稿状态引用 + 凭证缺失清单 + 发票缺失清单（总蓝图 H 阶段，v0.7 系列收官版）。
- 零迁移：outbox 行即持久幂等 marker（`daily-digest:{date}` / `friday-closeout:{date}`），重启不重发、当日补发、跨日不补、worker 离线顺延。

## 主要变更

见 `CHANGELOG.md` [0.7.7] 条目。要点：新模块 `backend/src/dailyDigest/`（digestContent 取数组装 / digestMessage fail-closed 渲染 / digestScheduler 独立 60s 循环）、`listNotices` 扩 `firstSeenFrom`、`outboxRepository.hasKey` 只读点查、`renderOutboxMessage` 增 `daily_digest`/`friday_closeout` 两分支、`GET /api/digest/status` + `POST /api/digest/run`（dryRun 预览/手动补发两分支）、`DAILY_DIGEST_*` 四配置键、审计三 action（actor=system:daily-digest）。

## 关键裁定（施工图 2026-08-28-v076-daily-digest-design.md，文件名 v076 为版本顺延前旧编号）

- 独立调度循环（不共用 v0.7.5 待办提醒 tick）：提醒是数据驱动扫描、晨报是时刻驱动，开关独立（`DAILY_DIGEST_AUTO_RUN` 生产默认开）、失败面隔离；三个调度器（招标/提醒/晨报）同构不同容。
- 幂等键一天一键不含内容哈希（同一天不允许第二条，内容随补发时刻漂移属预期）；`hasKey` 点查 + enqueue 同键异内容 409 反向保证 marker 强一致。
- 周报段只读引用不自动落库：Web 落库路径有模型副作用与人工 knowledgeIds 判断，系统代发会污染审计语义；用户要全文发"销售周报"、要落库去 Web 周报页。
- 晨报四段全空不发（宁静优先，audit skipped 留痕）；收尾包全空也发（"确认无欠账"是周五收尾的核心信息）。
- 发票缺失取 `unacknowledgedMissingCents > 0`（已确认"无票"的费用周五不再催）——与 Web 合计条徽标的 `invoice_pending` 状态数口径不同：微信收尾包催"未确认的缺口"，Web 徽标显示"发票状态为待补"的笔数，两者数字可能不同属预期。凭证缺失与 Web `paymentProofMissingCount` 完全同口径（无任何 payment_proof 附件）。

## 实施说明（与设计的偏差）

- ① 命名对齐现场指令：payload kind `weekly_wrapup` → `friday_closeout`；幂等键 `weekly-wrapup:{weekStart}` → `friday-closeout:{date}`（date=当周五业务日，手动补发在非周五也归一到本周五日期，一周一键语义不变）；审计 `digest.weekly_wrapup.sent` → `digest.friday.sent`；端点 `/api/assistant/digest/*` → `/api/digest/*`；配置统一 `DAILY_DIGEST_*` 前缀（`WEEKLY_WRAPUP_TIME` → `DAILY_DIGEST_FRIDAY_TIME`）。
- ② 设计"F 延期时待办段降级路径"作废不用：迁移 0028 与 actionItemStore 已上线，晨报待办段直接用 `remind_at` 判窗。
- ③ 设计第四章招标 lenient 单条容错**不实施**（现场裁定降级）：2026-08-20 陈账已于 v0.7.6 部署核验确认自愈（采集器连续 success、last_error=null、配置修复 AUTO_RUN=true）。观察项：如未来再现"单条坏公告整批拒绝"（症状为 scheduler `lastError="医院招标快照校验失败"` 持续），按施工图 §4.3 加固（sync lenient + internalRunner + scheduler 消费三件）。
- ④ 时刻配置解析失败按仓库 config 惯例 fail-fast 抛错（设计写"回默认"）；解析器接受自身输出以保持 loadConfig 幂等（服务器入口把已加载 config 回喂 createServer，对象值二次解析必须原样通过——实施中实测发现并修复）。
- ⑤ 设计引用的 adapter 内部函数 `riskRows` 非导出面，落地用导出的 `actionRiskSummary().risks`；行程"损坏 plan"因 DB 有 `json_valid` CHECK 不可能是非法 JSON，fail-open 判定改为"stops 非非空数组即仅显示标题"。
- ⑥ `resolveBusinessOwner` 无法解析（绑定账号 ≠ 配置 owner）按瞬态处理：不入队不标记不审计、下 tick 重试（设计未细化；误发"已齐 ✓"比迟发更糟）。
- ⑦ 改动面说明：本版冻结提交的历史包含同工作树另一会话在实施期间落入的 `f5337ee`（前端设置页记账实时日志导航修复 + navRoutes 双向一致性测试，v0.7 系列验收走查发现）与 `cfe66fd`(文档)。全部门禁（qa:local 416 项含其新增 routes 测试）在包含该修复的工作树状态上运行并通过，该修复随 v0.7.7 一并上线。

## 门禁（全绿）

| 门禁 | 结果 |
|---|---|
| backend `npm test` | 1275 通过 / 0 失败（v0.7.6 基线 1239，净增 36） |
| 前端 `qa:local` | 416 通过 / 0 失败；build 成功；密钥扫描 findings=[]（603 文件）；EXIT=0 |
| `qa:integration`（Chrome） | passed，EXIT=0 |
| `qa:webkit` | passed，EXIT=0 |
| 根 `test:deploy` | 249 通过 / 0 失败 / 2 平台跳过 |
| `git diff --check` | 干净 |

## 部署证据（2026-08-28 16:33–16:39 CST 完成切换）

| 字段 | 记录 |
| --- | --- |
| 冻结提交 / tag | `a3b5bbc6ecc1abd6fecf0c86be8517e3aca4ae72` / 本地注释 tag `v0.7.7`（不推 GitHub） |
| 串行门 | v0.7.6 postflight 25/25 passed + HTTPS 冒烟 25/25 passed（cleanup=clean）证据在位；current=v0.7.6 release；无 `.maintenance-lock`、无 cutover 进程，一次通过 |
| bundle | `staging/sentelligent-v0.7.7-a3b5bbc.bundle`，SHA-256 `891485ac9597c8212e5223f42062dbd5f49629dc000a9c10e27b9fdbc6a72280`（本机与服务器复算一致，600 权限） |
| 服务器打包 | `staging/build-v0.7.7-20260828T083304Z_a3b5bbc6ecc1/`（`LC_ALL=C`，bundle detached checkout 至冻结提交、status --porcelain 为零，release-package status=created、commit 匹配、clean=true、686 文件），归档 SHA-256 `6bf2800e16f70158b3e79a25e32f03b072f06f1c726092d739a9e43f98fcfc09` |
| release 目录（不可变冻结） | `/opt/sentelligent-sales-workbench/releases/v0.7.7-20260828T083304Z_a3b5bbc6ecc1`（root:root；`find -type d chmod 755` / `-type f chmod 644` 显式归一，根/backend/src/dist stat 抽查 755/644），manifest SHA-256 `6876b5498d1651dfd8eb3b56cea732e2398f7ecff35de28eb3cb07d4f2c8c9e5`，VERSION=0.7.7 |
| preflight | `25/25 passed`（对切换前 current v0.7.6 复核），报告 `…-preflight/preflight-report-20260828T083508Z.json` SHA-256 `b1b6059bd810a812d6879bde82c74d5502917b1834da16ce3de0684a20ff2309`；pre-cutover 在线快照（node:sqlite readOnly + VACUUM INTO）SHA-256 `601e53ef6af15cd6b0f70a303636a876b081c905d3a253d812917f5f188c7641` |
| cutover | `passed`（run `2026-08-28T08-35-27Z-14766`，首切一次通过、无回滚；本版零迁移，`schema_migrations` 保持 27 项 last=0028，日志 "Database migrated" 为幂等 runner 常规输出、无新迁移应用；离线备份、三服务重固定）；数据库离线备份 SHA-256 `eb8103b918ea8d9ce9cf2a931865044cb0e7fa1f508e9cbbbc3eea4ad282e1d0`；微信会话备份 SHA-256 `d66200aa115b4a782b2853771dd431c6d05a23efaba4fb52a466bc149e56a517` |
| postflight | `25/25 passed`（对新 current v0.7.7），报告 `…-postflight/postflight-preflight-report-20260828T083557Z.json` SHA-256 `74b58e8b882028d14055c325c47daee7e01a9f134cea1d00901c947a582a7a2a` |
| HTTPS 冒烟 | `25/25 passed`，`cleanup=clean`（residual 全零、quick_check ok），run ID `b055fcad-3379-4e12-abea-856f81f6b28c`，报告 SHA-256 `ac91235a8a67007c1036520f25b77c69d82f9f4d2e74a2c11c71cd7fb7bae9a5`；临时凭据经 `/dev/shm` 内存备份字节级恢复（`bytes-identical`，原 env SHA-256 `bb9d15af73d5b4a504cb6b4a16ee907acc8a07235c032f29f3ee0459aa971291`），恢复后健康 200；密码经 JSON stdin 传递，全程不落日志。过程偏差：首次冒烟脚本因 CRLF 行尾在 `set -o pipefail` 阶段即退出（未触及 env 与服务），转 LF 后一次通过 |
| 生产健康 | health `status=ok`、`database=ready`、`modelName=deepseek-v4-flash`、`modelReady=true`、`authEnabled=true`；HTTP→HTTPS 308；backend/frontend/weixin-agent 三服务 ExecStart 全部固定到 v0.7.7 release、四服务 active，共享 Caddy 未动；cutover 后 backend 无 error 日志、无新增失败 outbox |
| 晨报/周五包生产实录 | 切换完成于 16:35 CST（已过当日 16:30 门），digest 调度器（生产默认 AUTO_RUN=true）启动后首 tick 16:36:30 CST 按补发语义各投一条并均已真机送达（status=sent，provider_message_id 在录，16:36:31/32 CST）：`daily_digest` 幂等键 `daily-digest:2026-08-28`（audit lateMinutes=456；内容：新招标段 high 10 条/medium 6 条，行程/待办/风险空段省略）、`friday_closeout` 幂等键 `friday-closeout:2026-08-28`（audit lateMinutes=6；内容：周报无素材分支 + 发票缺失 1 笔合计 12.50 元 + 凭证缺失 0）。入队时刻早于冒烟合成数据窗口（16:38–16:39），内容为纯真实业务数据；冒烟期间两次 backend 重启均未重发（outbox marker 幂等生效）。招标调度器 `last_status=success`、`last_error=null`、cycle 65、下轮 17:20 CST；待办提醒队列 1 条未来项等待正常 |
| 回滚点 | `/opt/sentelligent-sales-workbench/releases/v0.7.6-20260828T073552Z_441a8b4b19e3`（commit `441a8b4b19e3d5cfa75f6737518276a38d277755`），未执行回滚 |

## 边界

- 不写真实业务数据（晨报/收尾包为系统自身推送职能，属正常运行；不为验证强发非幂等的重复真实推送）；不操作微信 UI；生产发布严格串行、不可变 release；密钥不落盘不入日志；共享 Caddy 不动；模型路由不变；`HOSPITAL_TENDER_AUTO_RUN`/`ACTION_REMINDER_*` 生产配置未动；零迁移。
