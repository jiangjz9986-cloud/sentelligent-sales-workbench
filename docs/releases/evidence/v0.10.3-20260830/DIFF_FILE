--- ORIGINAL_v0.10.3.md
+++ MODIFIED_FILE
@@ -1,32 +1,101 @@
-# 森特智行 v0.10.3 Web 小小对话面板
-
-## 目标
-
-- 消灭 Web/微信双端割裂：全站 Web 小小对话入口，复用 assistant runtime 与 pending-action 确认模型。
-- 实施任务书：`docs/superpowers/plans/2026-08-29-v0103-web-chat-design.md`。
-
-## 主要变更
-
-见 `CHANGELOG.md` [0.10.3]。零数据库迁移。
-
-## 门禁（全绿）
-
-| 门禁 | 结果 |
-|---|---|
-| backend `npm test` | 1409 通过 / 0 失败（基线 1386 +23） |
-| 前端 `qa:local`（CHROME_PATH） | 全绿（含 assistant-chat 守护 15 条） |
-| `qa:integration`（Chrome） | passed（周日记录日选择器去硬编码） |
-| `qa:webkit` | passed |
-| 根 `test:deploy` | 249 passed / 2 skipped |
-| secret 扫描 | findings=[] |
-| `git diff --check` | 干净 |
-| `rg confirmationCode` 前端助手目录 | 0 命中 |
-| 主 chunk（build dist） | `index-*.js` ≈ **373KB**（373479 字节） |
-
-## 部署证据
-
-待四关部署后回填。
-
-## 生产验收记录
-
-待 6 步验收后回填。
+# 森特智行 v0.10.3 Web 小小对话面板
+
+## 目标
+
+- 消灭 Web/微信双端割裂：全站 Web 小小对话入口，复用 assistant runtime 与 pending-action 确认模型。
+- 实施任务书：`docs/superpowers/plans/2026-08-29-v0103-web-chat-design.md`。
+
+## 主要变更
+
+见 `CHANGELOG.md` [0.10.3]。零数据库迁移。
+
+## 冻结身份与接管时间线
+
+| 字段 | 记录 |
+| --- | --- |
+| v0.10.3 冻结提交 / tag | `2dc9b1114107aee906d0ecfb006ac461cdb86cec` / `v0.10.3`；tag 未移动 |
+| 生产 release | `/opt/sentelligent-sales-workbench/releases/v0.10.3-20260829T164400Z_2dc9b11`；生产仍运行冻结版本 |
+| 当前开发分支 | `local/v0627-feature-closeout-20260827` |
+| 验收盘点基线 | `f38fd0d5d38b3cc80e7d2488e7a9e2b57e5efd25`；仅恢复 Cursor 审计/多账号设计文档，位于 v0.10.3 tag 之后 |
+| 后续 Web 热修提交 | `79f09d0738b4aff6aecd1cbc47effc337afdcba6`（`fix(assistant): close v0.10.3 web acceptance gaps`）；已本地提交、未部署，不改变 `v0.10.3` tag 或上述生产 release |
+
+本页生产专项验收结论来自冻结版本 `2dc9b111` 的生产实测。后续提交 `79f09d0` 的本地自动化结果只能证明热修候选在本地通过，不能改写已经观察到的生产结果，也不能计作生产复验通过。
+
+## 门禁（v0.10.3 冻结前全绿）
+
+| 门禁 | 结果 |
+|---|---|
+| backend `npm test` | 1409 通过 / 0 失败（基线 1386 +23） |
+| 前端 `qa:local`（CHROME_PATH） | 全绿（含 assistant-chat 守护 15 条） |
+| `qa:integration`（Chrome） | passed（周日记录日选择器去硬编码） |
+| `qa:webkit` | passed |
+| 根 `test:deploy` | 249 passed / 2 skipped |
+| secret 扫描 | findings=[] |
+| `git diff --check` | 冻结交易干净 |
+| `rg confirmationCode` 前端助手目录 | 0 命中 |
+| 主 chunk（build dist） | `index-*.js` ≈ **373KB**（373479 字节） |
+
+## 部署证据（2026-08-30 00:45 CST）
+
+| 字段 | 记录 |
+| --- | --- |
+| preflight（切换前 current=v0.10.2） | `passed`，25/25；报告 `/opt/sentelligent-sales-workbench/evidence/v0.10.3-20260829T164400Z_2dc9b11-preflight/preflight-report.json`；SHA-256 `381ecd52a7807ef552fcbca07844b31325b9c259fe9f8b5883f41ed4e807530a` |
+| cutover | `passed`，run `2026-08-29T16-45-27Z-16451`；报告 `/opt/sentelligent-sales-workbench/evidence/2026-08-29T16-45-27Z-16451/cutover-report.json`；SHA-256 `39c1e1acf2a5aca0f45975d974203f5228f86c25d5989c74eab49094df080e41` |
+| postflight | `passed`，25/25；报告 `/opt/sentelligent-sales-workbench/evidence/v0.10.3-20260829T164400Z_2dc9b11-postflight/preflight-report.json`；SHA-256 `225e409429eea22d9f50416b0c62b189c6fa98dbc87c7db9ba1b1569e8ec5f83` |
+| HTTPS 冒烟 | `passed`，25/25，0 blocked；run `dad372d2-f962-402e-8d24-00056c0c7ed9`；`cleanup=clean`；报告 SHA-256 `b53a5a4751a669cdbb9738d06e034d2079b5b5569e8dd2dc16ffe12b44cfd6af` |
+| 冒烟清理 | audit manifest 12 项通过；删除合成 customers/opportunities/quickRecords/aiInsights/salesDecisions/itineraries/weeklyReports/auditLogs/authSessions，残留均为 0；SQLite `quick_check=ok`，外键违规 0 |
+| 生产只读快照 | `current` 指向上述 v0.10.3 release；`VERSION=0.10.3`；`/api/health` HTTP 200；backend/frontend/weixin-agent 均 active |
+| 回滚点 | 切换前 production current 为 v0.10.2；应用回滚应切回其不可变 release，数据库恢复与应用回滚分开处理 |
+
+**部署四关判定：通过。** 以下专项功能验收必须独立判定，不能用四关、健康检查或通用 HTTPS smoke 代替。
+
+## 生产专项验收记录（2026-08-30）
+
+### 总判定：**未通过，存在用户动作阻塞**
+
+| 任务书 §7.2 项 | 实际输入与可见结果 | 判定 | 证据 |
+| --- | --- | --- | --- |
+| 打开 Web 小小面板 | 生产面板可打开，输入框与对话历史可见 | **已通过** | `screenshots/01-web-xiaoxiao-panel-open.png` |
+| 1. `帮助` | 返回完整能力列表 | **已通过** | `screenshots/02-help-passed.png` |
+| 2. 查询客户摘要 | 合成前缀 `V0103-UAT-20260830` 在客户列表为 `0 / 1 家客户`；输入 `查客户 V0103-UAT-20260830-客户` 后返回 `暂时无法识别这个请求，请换一种说法。`。命令 alias 未识别；同时未获准创建生产合成客户，无法继续验证已知客户摘要卡 | **未通过**；后续已知客户数据步骤被阻塞 | `screenshots/04-synthetic-customer-precondition-none.png`、`screenshots/05-synthetic-customer-query-blocked.png` |
+| 3–4. 待办完成确认、toast、列表和总览刷新 | 合成前缀在动作列表为 `0 / 4 个动作`；未获准创建生产合成待办，未生成/点击确认卡，未改任何动作或总览数据 | **被阻塞** | `screenshots/03-synthetic-action-precondition-none.png` |
+| 5. Web 财务只允许微信 | 严格输入 `记一笔午餐 50`，实际返回 `暂时无法识别这个请求，请换一种说法。`；未返回任务书要求的 HTTP 403 或明确“财务仅微信”拒绝文案，也未生成确认卡或财务记录 | **未通过** | `screenshots/06-finance-web-result-generic-unrecognized.png` |
+| owner 隔离 | 第二账号尚未用同一 `pendingActionId` 验证 404；当前也没有可用的合成 pending action | **被阻塞** | 尚无通过证据 |
+| 6. 微信同账号回归 | 绑定页显示 `机器人绑定：未开始`、`尚未生成二维码`、`服务：已连接`、`启动时间：尚无`、`更新时间：尚无`；只读日志另有 `context_token_missing` | **被阻塞** | `screenshots/07-weixin-binding-not-started.png` |
+
+### 七张截图的证据分类
+
+- **通过 2 张**：`01` 面板打开、`02` HELP 返回。
+- **失败 2 张**：`05` 客户查询命令未识别、`06` 财务命令没有得到明确的 Web 403 拒绝。
+- **阻塞/前置状态 3 张**：`03` 无合成待办、`04` 无合成客户、`07` 微信尚未扫码绑定。
+- 失败与阻塞截图只证明生产实际结果，不得计入通过项；owner 隔离目前没有通过截图。
+
+### 生产数据与影响面
+
+- 本轮没有创建、更新、确认或删除客户、待办、财务记录或其他业务实体。
+- 合成前缀残留检查：客户 `V0103-UAT-20260830` 为 0；待办 `V0103-UAT-20260830` 为 0。
+- 仅产生 Web 小小对话测试消息；没有产生业务写入确认卡或业务实体。
+- 本机已保存的 production-smoke 登录密码进行唯一一次验证时返回 `401 INVALID_CREDENTIALS`，判定该本机凭据已陈旧；本轮未重试、未改生产密码、未重启服务。
+
+## 后续本地 Web 热修状态（不等于生产复验）
+
+`79f09d0738b4aff6aecd1cbc47effc337afdcba6` 已修复本次生产实测暴露的客户命令 alias、`记一笔` 财务词法、Web 工具 allowlist fail-open、成功结果展平和 403 明确拒绝气泡问题。该提交的独立证据位于 `docs/evidence/v0.10.3-web-acceptance-hotfix-20260830/`。
+
+本地门禁记录为：目标后端 64/64、后端全量 1411/1411、前端 assistant-chat 17/17、根部署测试 249 passed / 2 skipped、Secret Scan `findings=[]`，完整 11 路径临时 index 使用 `git -c core.whitespace=cr-at-eol diff --cached --check` 得到 exit 0。热修文件保留原有 CRLF/LF 风格；历史上的普通 `git diff --check` 对五个 CRLF 文件返回 exit 2 并将 CR 报为 trailing whitespace，该历史结果保留在证据中，本页不声称普通检查对这些 CRLF 文件通过。
+
+上述结果全部来自本地自动化。`79f09d0` 未部署，生产仍是 `2dc9b111`；因此生产专项验收状态仍为“未通过，存在用户动作阻塞”，需在后续不可变版本部署后重新执行真实生产验收。
+
+## 需用户完成或即时确认的动作
+
+1. 明确确认是否允许创建并在验收结束后清理生产合成客户与合成待办；只使用唯一 `V0103-UAT-20260830-<HHMMSS>` 前缀，不使用真实客户/待办替代。
+2. 在“系统配置 → 微信绑定”生成二维码并扫码，使微信登录态恢复后再执行同账号回归。
+3. 更新本机 Keychain 中已陈旧的 production-smoke 密码；不得把密码写入文档、日志或命令行参数。
+4. 取得上述确认并完成热修版本部署后，按顺序补齐：客户摘要 → 待办确认卡 → 用户即时确认后点确认 → toast → 动作列表 → 总览刷新 → 第二账号 404 → 再次即时确认后清理 → 两类前缀残留均为 0。
+
+## 证据目录
+
+专项证据统一位于：
+
+`docs/releases/evidence/v0.10.3-20260830/`
+
+目录包含部署只读快照、四关结构化摘要、七张生产截图及 SHA-256、证据索引、原始发布文档及 SHA-256、`MODIFIED_FILE`、`DIFF_FILE`、`VERIFICATION.txt` 和可执行 `ROLLBACK.sh`。本次证据收口未执行生产业务写、部署、服务重启、push 或 tag 移动。
