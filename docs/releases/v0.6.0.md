# 森特智行 v0.6.0 发布说明

## 定位

v0.6.0 把系统配置与医院招标监测从独立代码能力收口为主系统功能。GitHub Release、生产切换和线上验收均已完成。

## 正式发布记录

- tag：`v0.6.0`，提交：`4c45656647f5d6d4c28e011fd58eb954735cad64`。
- GitHub Release：[森特智行 v0.6.0](https://github.com/jiangjz9986-cloud/sentelligent-sales-workbench/releases/tag/v0.6.0)。
- Release workflow：`31998568637`，验证与发布 job 均成功。
- 归档 SHA-256：`3b4f747384ecd594aa9db0a337aee3d3f239432e89a13c14cb63678e69c5f371`。
- 生产状态：已切换到 `v0.6.0`；当前 release 为 `/opt/sentelligent-sales-workbench/releases/v0.6.0-20260817T124347Z_4c45656647f5`，回滚点为 `/opt/sentelligent-sales-workbench/releases/v0.5.7-20260816T094633Z_d4fcb56504c5`。

## 生产切换记录

- 生产已准备并核验项目独享 Python 3.12.14：`/opt/sentelligent-sales-workbench/runtime/python-3.12.14/bin/python3.12`。
- 已完成安全设置主密钥注入、数据库/环境/微信会话备份和 `0014`、`0015`、`0016` 迁移；密钥值、备份内容和业务数据不进入本文档。
- 切换前、切换后生产预检均为 `25/25`；后端与前端健康检查均为 `200`，DeepSeek、OCR/PDF 工具和 SQLite 完整性检查通过。
- 医院招标 scheduler 已建立并启用：`enabled=1`、`interval=60` 分钟、`batch=10`、`cycle=0`、`status=idle`。它会按稳定客户序号继续轮巡，整批成功后才推进游标。
- Chrome 生产页面回归已覆盖战情总览、医院招标监测、系统配置和微信页面；只重启后端、前端、微信三个项目服务，共享 Caddy 和同机受保护服务未重启或改配。
- HTTPS smoke 已完成：run ID `f19df464-c651-4378-ae06-d46fa198897b`，`25/25`、`failed=0`、`blocked=0`、`cleanup=clean`；报告路径为 `/opt/sentelligent-sales-workbench/evidence/v0.6.0-20260817T124347Z/smoke/production-https-smoke-20260817T140107Z.json`，SHA-256 为 `45de5d14e8f732d9c162f62e79ebea61d2f5080deb03a31b52620f3cd795a150`。
- 清理后所有合成资源残留计数均为 `0`，SQLite `quick_check=ok`、外键违规 `0`。

## 主要变化

- 新增独立系统配置页，支持 iCost Token 一次性生成/轮换和 DeepSeek API Key 加密保存。
- 浏览器、微信及持久化助手统一通过服务端密钥提供器调用模型。
- 医院招标监测使用内置公开来源采集器，不要求使用者配置第三方招标 API。
- 默认每 60 分钟处理 10 个客户；每轮只采集一次公开快照，后续批次复用并合并客户匹配。
- SQLite 保存轮巡启停、批量、游标、轮次、快照、运行记录、锁、最近/下次运行和安全错误。
- 页面显示轮巡进度、本批新增高相关公告、来源健康、公告详情和客户匹配证据。

## 安全与业务边界

- 密钥明文不进入普通查询、日志、审计、浏览器存储或 Git。
- 只有登录用户和 CSRF 校验通过的写请求可以修改配置或手动触发下一批；采集机器身份不能读取轮巡状态或调用控制入口。
- 招标情报只读，不自动创建或修改客户、商机、动作、风险或销售阶段。
- 批次部分失败不推进游标；服务重启、客户新增/删除和锁冲突按持久化状态恢复。

## 当前验证

- 后端全量测试通过：712/712。
- 根级部署测试通过：222 个通过，2 个 Windows 专用测试跳过。
- 前端 API、医院招标页面契约、生产构建和 `qa:local` 通过。
- 完整 Git 历史密钥扫描通过，无发现。
- Chromium 原生 `qa:integration` 已覆盖 1440×900、1920×1080、1366×768、834×1194、390×844 和 360×800 视口；`qa:webkit` 也已通过。以上仍是本地候选证据，不替代生产 HTTPS smoke 和上线观察。

## 生产验收

- 配置 `SETTINGS_ENCRYPTION_KEY`。
- 将 `HOSPITAL_TENDER_PYTHON` 指向生产服务器上规范化、非符号链接、无 setuid/setgid 与文件 capability、后端服务账号可执行但不可改写的 Python 3.11+ 绝对路径。
- 生产环境明确配置 `HOSPITAL_TENDER_AUTO_RUN=true`、`HOSPITAL_TENDER_INTERVAL_MINUTES=60`、`HOSPITAL_TENDER_BATCH_SIZE=10`；`0016` 迁移会在 SQLite 中持久化固定的 `60/10` 默认状态。
- 轮巡环境变量只作为发布合同及持久化值缺失或无效时的回退，不会覆盖已有 scheduler 状态。如需非默认间隔或批量，由已登录用户通过受 CSRF 保护的 `PATCH /api/hospital-tenders/scheduler` 持久化设置。
- 服务器 Python 3.11+ 运行时准备、数据库迁移演练、生产预检、受控切换、页面/微信回归和 HTTPS smoke 均已完成；本版本可作为当前生产验收基线。
