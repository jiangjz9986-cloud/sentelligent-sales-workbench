# 森特智行 v0.2.1

版本条目建立时间：`2026-07-29T03:29:14+08:00`

## 状态判定

`v0.2.1` 已完成合并、标签发布和生产部署，是当前生产基线。

| 项目 | 事实 |
| --- | --- |
| `main` 提交 | `f8d43bbfd6172828340a270c5276485192223a65` |
| annotated tag | `v0.2.1`，`2026-07-29T03:27:14+08:00` 创建 |
| GitHub Release | [森特智行 v0.2.1](https://github.com/jiangjz9986-cloud/sentelligent-sales-workbench/releases/tag/v0.2.1)，`2026-07-29T03:29:14+08:00` 发布 |
| 归档 SHA-256 | `fbcd705dd28257faec3139f31837bb8562e6f7405b770c402d83c8746789a815` |
| 生产地址 | `https://82.156.210.199/` |
| 生产 release 目录 | `/opt/sentelligent-sales-workbench/releases/2026-07-29_f8d43bb` |
| 回滚 release 目录 | `/opt/sentelligent-sales-workbench/releases/2026-07-28_f89e1e7` |

本次文档更新没有连接生产环境，也没有重新执行线上检查。服务 PID、配置哈希、数据库备份和线上验收结果以 [部署记录](../部署记录.md) 及服务器受保护 evidence 为准。

## v0.2.0 发布事故

- annotated tag `v0.2.0` 固定指向提交 `591e48d464341d1df95f541d84790e7452341d5d`。
- 该 tag 是不可移动的发布身份，不得删除、改指向或复用于后续发布。
- Release workflow run [`30389038587`](https://github.com/jiangjz9986-cloud/sentelligent-sales-workbench/actions/runs/30389038587) 在 `Verify release tooling and source boundaries` 阶段失败。
- 失败没有创建 `v0.2.0` GitHub Release，也没有产生可供生产部署的正式 Release 资产。
- 后续修复使用新版本 `v0.2.1`，保留 `v0.2.0` 的失败证据和审计链。

## 根因

WSL 辅助脚本使用宿主平台的 `path.resolve()` 处理 Windows 绝对路径。Linux runner 不把 `C:/...` 识别为本机绝对路径，因而将其错误解析为 `<cwd>/C:/...`，造成 6 项跨平台路径测试失败，并阻断 `v0.2.0` Release workflow。

## 热修复内容

- 增加跨宿主工作区路径解析：带盘符的 Windows 绝对路径使用 `path.win32` 规范化，POSIX 绝对路径使用 `path.posix` 规范化，相对路径才交给宿主平台 `path.resolve()`。
- 增加跨宿主路径拼接：Windows 和 POSIX 绝对路径分别使用对应平台的 `join()`，避免 Linux 路径被误转为反斜杠形式。
- UNC 网络共享和 Windows 根相对工作区路径不能可靠转换为 WSL 工作目录，现改为明确失败并提示映射盘符或传入已挂载的 POSIX 路径。
- 本地开发、WSL 后端和 WSL 全栈配置入口统一采用同一套路径解析规则。
- PR CI 的根测试门禁与 Release workflow 对齐为 `node --test scripts/*.test.mjs`，确保发布工具、生产预检、秘密扫描和 WSL 辅助脚本在 Linux runner 上完整执行。
- 根凭据扫描命令显式传入 `--history`，即使扫描器未来修改默认值，PR CI 仍与标签发布保持完整历史范围。
- 根项目、后端和前端正式版本统一为 `0.2.1`。

## 验证与发布证据

- `v0.2.1` 的修复提交已进入 `main`，并通过标签发布 workflow 生成 `release-result.json`、源码归档和 `SHA256SUMS`。
- 候选阶段的完整 `qa:full`、全历史凭据扫描、根测试、后端测试和前端本地/集成质量门已按开发日志记录完成；后续代码变更不能直接复用这些结果作为 `v0.2.2` 证据。
- 生产部署使用独立 release 目录，服务只固定到该目录；共享 Caddy 和同机其他系统服务不随应用发布重启。

## AI 功能边界

《森特智行复杂型 B2B 销售决策 Agent V1》规则规格已经完成，规格级检查也已通过。但这份完整规格尚未拆分为核心 system prompt、场景 playbook、行业规则包、JSON Schema 和 eval 集并接入现有 DeepSeek 运行时，因此不能写成 `v0.2.1` 已上线能力。现有快速记录 DeepSeek 分析链路独立运行，业务写回仍需人工确认。

## 回滚

回滚目标为 `/opt/sentelligent-sales-workbench/releases/2026-07-28_f89e1e7`。执行回滚时，三个项目 systemd 单元必须重新固定到已验收 release 的真实路径，并重新执行生产预检和公开 HTTPS 冒烟；数据库默认不随代码回滚。共享 Caddy、account-vault、Qingyang 和 Mihomo 不属于应用回滚范围。
