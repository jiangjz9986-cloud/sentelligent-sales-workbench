# v0.4.1 Production Cutover Contract Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development and verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the production preflight and cutover validators accurately accept the existing hardened Sentelligent service units while preserving strict separation from private backend credentials and unrelated services.

**Architecture:** Keep the three project services and shared Caddy outside the mutation boundary. Represent the known unit surface explicitly: `PrivateTmp=true`, no extra sandbox/path directives, backend/weixin sharing only the backend environment file, frontend consuming only its public runtime environment, and the WeChat worker's fixed non-secret `HOME` assignment. Add regression tests before changing either validator, then ship the contract as a patch release.

**Tech Stack:** POSIX shell, Node.js `node:test`, JSON service snapshots, GitHub Release archive, systemd.

---

### Task 1: Lock the current unit contract with failing tests

**Files:**
- Modify: `scripts/production-cutover.test.mjs`
- Modify: `scripts/production-preflight.test.mjs`

- [x] Add a cutover fixture assertion showing `PrivateTmp=true` and the frontend `EnvironmentFile` are accepted while additional environment files remain rejected.
- [x] Add a preflight service-snapshot assertion showing the same contract and the exact WeChat `HOME` assignment are accepted.
- [x] Run both targeted tests and confirm they fail for the current validator behavior, including the CentOS 7 singular `EnvironmentFile=` output key.

### Task 2: Implement the smallest validator correction

**Files:**
- Modify: `scripts/production-cutover.sh`
- Modify: `scripts/production-preflight.mjs`

- [x] Permit only the approved `PrivateTmp=true` hardening value; keep `PrivateDevices=false`, `DynamicUser=false`, `ProtectSystem=no`, and `ProtectHome=no` requirements.
- [x] Require backend and WeChat services to reference the same canonical backend environment file; require frontend to reference only the canonical frontend environment file and reject the backend file.
- [x] Permit only the fixed WeChat `HOME=/opt/sentelligent-sales-workbench/weixin-session` assignment; keep all other unexpected environment assignments rejected.
- [x] Run the targeted tests and confirm they pass.

### Task 3: Bump and document the patch release

**Files:**
- Modify: `VERSION`
- Modify: `package.json`
- Modify: `backend/package.json`
- Modify: `outputs/product-design-prototype/package.json`
- Modify: `CHANGELOG.md`
- Modify: `docs/v0.4.0-development-handoff.md`
- Modify: `docs/部署记录.md`

- [x] Set the patch version to `0.4.1` and record the validator correction without recording any secret value.
- [ ] Add the exact production acceptance and rollback evidence fields that must be recorded after cutover.

### Task 4: Fresh release and production verification

**Files:**
- No source files beyond the release metadata above.

- [x] Run the full release/deploy tests, backend tests, frontend local/Chromium/WebKit suites, diff check, and history secret scan.
- [ ] Create and hash the GitHub Release archive from the merged commit.
- [ ] Upload and verify an immutable server release, generate a fresh `24/24` preflight, and run the guarded cutover for only the three project services.
- [ ] Run `25/25` HTTPS smoke and real Chrome read-only acceptance, then record evidence and create the non-secret desktop handoff folder.
