# v0.2.4 Model Stage Normalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preserve valid DeepSeek sales-decision analysis while failing closed on unknown model stage labels.

**Architecture:** Keep current stage fully server-owned. Accept a model-recommended stage only when it is already a canonical `sales-decision-v1` value; otherwise retain the current stage without discarding unrelated valid analysis.

**Tech Stack:** Node.js 24, built-in `node:test`, GitHub Actions/Releases, systemd, SQLite.

---

### Task 1: Normalize The Complete Stage Boundary

**Files:**
- Modify: `backend/src/ai/agents/salesDecisionAgent.js`
- Modify: `backend/tests/sales-decision-agent.test.js`

- [x] Write a failing test with non-canonical current and recommended business stages.
- [x] Verify the test fails with `mock_model_fallback`.
- [x] Override current from deterministic evidence and fall unknown recommended back to current.
- [x] Verify the focused suite passes while invalid JSON still falls back.

### Task 2: Release And Deploy

**Files:**
- Modify: `VERSION`, package manifests and locks, `CHANGELOG.md`
- Create: `docs/releases/v0.2.4.md`

- [ ] Run complete local, browser, secret, version, and package verification.
- [ ] Publish through PR, main CI, annotated tag, and GitHub Release.
- [ ] Deploy only the verified Release archive with the controlled cutover.
- [ ] Require production preflight `19/19`, HTTPS smoke `25/25` with clean cleanup, browser acceptance, and protected-service stability.
