# v0.4.2 Production Preflight Boundary Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development and verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the remaining production-preflight boundary gaps, publish an immutable patch release, and complete the guarded three-service production cutover.

**Architecture:** Keep schema-3 releases fully inventory-bound and restrict the already-scoped schema-2 current-release compatibility path to its exact hashed archive inventory; no unverified dependency may be added. Bind the frontend service to the one canonical public environment file. Require every release tree to be root-owned and runtime-read-only, reject candidates that were not frozen before complete hash verification, and preserve the cutover boundary around backend, frontend, and WeChat; Caddy and unrelated services remain read-only protected state.

**Tech Stack:** Node.js 24, `node:test`, POSIX shell, systemd, SQLite, GitHub Actions/Releases.

---

### Task 1: Reject unverified schema-2 dependencies

**Files:**
- Modify: `scripts/production-preflight.test.mjs`
- Modify: `scripts/production-preflight.mjs`

- [x] Build a genuine schema-2 fixture without schema-3 production dependencies, then add one unverified `backend/node_modules/` file both outside and inside the claimed archive count.
- [x] Run the targeted tests and observe that both unverified dependency cases are incorrectly accepted.
- [x] Require the exact schema-2 archive file count and reject every file absent from its verified hash inventory.
- [x] Run the targeted tests and the complete preflight suite; expect zero failures.

### Task 2: Require the canonical frontend environment file

**Files:**
- Modify: `scripts/production-preflight.test.mjs`
- Modify: `scripts/production-preflight.mjs`

- [x] Add a test that changes both frontend environment fields to `/tmp/config/frontend.env` and expects `validateProjectServices()` to reject it.
- [x] Run the targeted test and observe that the unsafe suffix-matching path is accepted.
- [x] Replace suffix matching with equality to `/opt/sentelligent-sales-workbench/config/frontend.env`.
- [x] Run the targeted test and the complete preflight/cutover suites; expect zero failures.

### Task 3: Close release ownership and freeze-order gaps

**Files:**
- Modify: `scripts/production-preflight.test.mjs`
- Modify: `scripts/production-preflight.mjs`
- Modify: `scripts/production-cutover.test.mjs`
- Modify: `scripts/production-cutover.sh`
- Modify: `scripts/delivery-guide.test.mjs`
- Modify: `docs/发布与回滚操作手册.md`

- [x] Add failing tests proving legacy ownership relaxation, candidate verification before freezing, and runtime-owned staging instructions.
- [x] Require root ownership for schema-2 and schema-3 releases, reject candidates that are not already frozen before manifest verification, reject every candidate disk file outside the manifest inventory, and document root-owned extraction plus one-time legacy freezing against an externally trusted manifest SHA-256.
- [x] Run the targeted ownership, ordering, and documentation tests; expect zero failures.

### Task 4: Publish the patch release

**Files:**
- Modify: `VERSION`
- Modify: `package.json`
- Modify: `backend/package.json`
- Modify: `backend/package-lock.json`
- Modify: `outputs/product-design-prototype/package.json`
- Modify: `outputs/product-design-prototype/package-lock.json`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `docs/v0.4.0-development-handoff.md`
- Create: `docs/releases/v0.4.2.md`

- [x] Set every release-facing version to `0.4.2` and make README links point to the patch branch/release.
- [x] Run the root release/deploy suite, backend suite, frontend local/Chromium/WebKit QA, `git diff --check`, version locks, and the history secret scan.
- [ ] Commit, push, open a ready PR, wait for required checks, merge, tag the exact merge commit, and verify the GitHub Release archive/manifest/hash.

### Task 5: Complete production cutover and acceptance

**Files:**
- Modify: `docs/部署记录.md`

- [ ] Stage the exact release archive under a new immutable server release directory and verify its manifest and SHA-256.
- [ ] Verify the fresh SQLite backup (`DELETE`, no sidecars, `quick_check=ok`, zero foreign-key violations), generate the service plan, and produce a fresh exact `24/24` preflight.
- [ ] Run `production-cutover.sh` for only backend, frontend, and WeChat; retain Caddy, Qingyang, account-vault, Mihomo, and port `8797` unchanged.
- [ ] Generate a post-cutover `24/24` preflight, run `25/25` HTTPS smoke with clean test-data cleanup, and complete read-only real Chrome acceptance.
- [ ] Record non-secret deployment evidence in `docs/部署记录.md`, submit the deployment-record PR, and create a non-secret desktop handoff folder.
