# v0.3.1 UI Polish Closeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan.

**Goal:** Finish and verify the incomplete WorkBuddy/Kimi frontend polish without losing its existing uncommitted changes or changing established product behavior.

**Architecture:** Keep the existing React page/component structure and complete the redesign primarily through `App.jsx`, shared workbench page composition, and `global.css`. Preserve existing `data-testid` values, copy contracts, and backend APIs. Any newly discovered defect must first be captured by a failing automated test, then fixed with the smallest production change.

**Tech Stack:** React 19, Vite 6, CSS, Node test runner, Chromium/WebKit QA

---

### Task 1: Preserve and document the inherited UI changes

**Files:**
- Inspect: `C:/Users/50159/WorkBuddy/2026-08-01-00-13-40/.workbuddy/memory/2026-08-01.md`
- Inspect: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/App.jsx`
- Inspect: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/features/salesWorkbench/pages.jsx`
- Inspect: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/styles/global.css`
- Inspect: `D:/Codex项目/森特智行/outputs/product-design-prototype/scripts/integration-qa.mjs`
- Inspect: `D:/Codex项目/森特智行/outputs/product-design-prototype/scripts/visual-rhythm.test.mjs`

**Step 1: Record the baseline**

Run: `git status -sb` and `git diff --stat`

Expected: only the five inherited WorkBuddy/Kimi frontend files are modified.

**Step 2: Confirm the design intent**

Compare the WorkBuddy notes, generated screenshots, and current diff. Verify the intended command-center sidebar, light content surface, compact sub-view headings, two-column quick-record workspace, and full-width record history.

**Step 3: Confirm the inherited behavior baseline**

Run the frontend Node test aggregate, the independent customer-opportunity contract test, and a production build.

Expected: all inherited tests pass before any new fix is introduced.

### Task 2: Restore a reliable local visual preview

**Files:**
- Inspect: `D:/Codex项目/森特智行/scripts/local-dev.mjs`
- Runtime only: `D:/Codex项目/森特智行/.runtime/local-dev.json`

**Step 1: Remove stale script-owned runtime state**

Run: bundled Node with `scripts/local-dev.mjs stop`.

Expected: stale PIDs and the obsolete runtime record are removed without modifying source files.

**Step 2: Start the supported local stack**

Run: bundled Node with `scripts/local-dev.mjs start`, then `scripts/local-dev.mjs health`.

Expected: backend health succeeds on port 8897 and the frontend responds on port 5184.

**Step 3: Inspect the real UI**

Open the local frontend in the in-app browser. Check login, overview, quick record, detail/edit sub-views, and responsive behavior at desktop and narrow mobile widths. Capture screenshots for evidence where useful.

### Task 3: Close any visual or interaction gaps with TDD

**Files:**
- Modify if needed: `D:/Codex项目/森特智行/outputs/product-design-prototype/scripts/visual-rhythm.test.mjs`
- Modify if needed: `D:/Codex项目/森特智行/outputs/product-design-prototype/scripts/integration-qa.mjs`
- Modify if needed: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/App.jsx`
- Modify if needed: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/features/salesWorkbench/pages.jsx`
- Modify if needed: `D:/Codex项目/森特智行/outputs/product-design-prototype/src/styles/global.css`

**Step 1: Add one focused failing regression per discovered defect**

Run the new test alone and confirm it fails for the intended reason.

**Step 2: Implement the smallest fix**

Preserve existing test IDs, product copy, API behavior, and responsive contracts.

**Step 3: Prove the regression is fixed**

Run the focused test again, then the relevant nearby suite.

Expected: both pass with no unrelated snapshot or contract changes.

### Task 4: Perform full verification

**Files:**
- Verify: `D:/Codex项目/森特智行/outputs/product-design-prototype/`

**Step 1: Static and unit checks**

Run: `git diff --check`, all frontend Node tests, and the production build.

Expected: zero failures and no whitespace errors.

**Step 2: Browser integration checks**

Run the official Chromium integration QA across all configured viewports.

Expected: login/logout, CRUD, quick record, generated actions/risks, knowledge, weekly report, sales decision, disabled-feature boundaries, and optimistic-lock conflict handling all pass with no horizontal overflow.

**Step 3: WebKit/mobile checks**

Run the repository WebKit QA and visually inspect its narrow-screen screenshots.

Expected: no clipped content, overlapping controls, undersized touch targets, or broken degraded/readonly states.

**Step 4: Broader regression checks**

If shared product files changed beyond the inherited UI set, run the root and backend test suites required by the repository instructions.

### Task 5: Prepare the release handoff

**Files:**
- Update only if release is authorized: version/changelog/deployment records required by repository release tooling.

**Step 1: Report the finished local delta**

Summarize inherited Kimi changes, any additional fixes, and fresh verification evidence.

**Step 2: Keep publishing separate from implementation**

Do not commit, push, tag, create a GitHub release, or deploy to production unless the user authorizes that external release action after reviewing the completed result.
