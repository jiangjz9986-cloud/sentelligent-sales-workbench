# v0.4.3 CentOS 7 Cutover Compatibility Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development and verification-before-completion. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish an immutable v0.4.3 patch that makes the guarded production cutover compatible with the live CentOS 7 systemd surface, then deploy and accept that exact release without changing unrelated services.

**Architecture:** Keep every existing v0.4.2 release, preflight, ownership, database, and protected-service gate intact. Capture successful full `systemctl show <service>` snapshots and extract allowlisted properties locally, avoiding unsupported single-property selectors on CentOS 7; treat the absent `DynamicUser` field on systemd 219 as unsupported while still rejecting enabled or unknown values. Publish from the exact merged `main` commit and stage a new root-owned release instead of mutating or reusing v0.4.2.

**Tech Stack:** Node.js 24, `node:test`, POSIX shell, CentOS 7 systemd, SQLite, GitHub Actions/Releases, Chrome.

---

### Task 1: Prove the CentOS 7 regressions and minimal fixes

**Files:**
- Modify: `scripts/production-cutover.test.mjs`
- Modify: `scripts/production-cutover.sh`

- [x] Make the systemctl fixture fail whenever the script supplies a single-property selector and emit the complete production-compatible property surface only for `systemctl show <service>`.
- [x] Run the focused test before the implementation and observe failure because the cutover script still requests unsupported single properties.
- [x] Read one complete systemd snapshot per property lookup, retain support for the legacy singular `EnvironmentFile` output key, and keep duplicate or extra values fail-closed.
- [x] Run the focused test after the implementation and expect one passing test with zero failures.
- [x] Reproduce the systemd 219 omission of `DynamicUser`, then accept only an absent field or `no` and prove `yes` remains rejected.
- [x] Run the entire production-cutover test file (`30/30`) and `git diff --check`.

### Task 2: Validate the live CentOS 7 surface without cutover

**Files:**
- Server evidence only: `/opt/sentelligent-sales-workbench/evidence/`

- [x] Read the three project units with full `systemctl show <service>` calls and verify the properties required by the patched parser are present.
- [x] Exercise only the cutover script's systemd surface assertion against the live snapshots; do not stop, restart, reload, install units, access secret values, or change the database.
- [ ] Record a non-secret pass/fail evidence file containing timestamps, service names, queried property names, and result counts only.

### Task 3: Prepare the immutable v0.4.3 source release

**Files:**
- Modify: `VERSION`
- Modify: `package.json`
- Modify: `backend/package.json`
- Modify: `backend/package-lock.json`
- Modify: `outputs/product-design-prototype/package.json`
- Modify: `outputs/product-design-prototype/package-lock.json`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `docs/v0.4.0-development-handoff.md`
- Create: `docs/releases/v0.4.3.md`

- [x] Change every release-facing version from `0.4.2` to `0.4.3` without changing dependency versions.
- [x] Document the exact CentOS 7 compatibility fix, unchanged schema/API boundary, new tag requirement, and prohibition on reusing the staged v0.4.2 candidate.
- [x] Run version-lock checks and verify every v0.4.3 reference points to the new branch, release note, or tag.

### Task 4: Verify, review, and publish v0.4.3

**Files:**
- Verify all tracked release inputs.

- [x] Run `npm run qa:full` with Node.js 24 and confirm every root, backend, frontend, Chromium, and WebKit gate passes.
- [x] Run `git diff --check`, the full-history secret scan, and an independent code/release review; resolve every blocking finding and rerun affected gates.
- [ ] Commit intentional files, push `codex/v0.4.3-centos-cutover-compat`, open a ready PR, wait for required checks, and merge it into `main`.
- [ ] Create annotated tag `v0.4.3` from the exact merged `main` commit, wait for the Release workflow, and verify archive, manifest, API digest, and `SHA256SUMS` agree.

### Task 5: Deploy and accept the exact v0.4.3 release

**Files:**
- Modify after acceptance: `docs/部署记录.md`
- Modify after acceptance: `README.md`

- [ ] Create a new root-owned v0.4.3 release directory from the verified GitHub Release archive; do not modify or reuse either v0.4.2 candidate or the frozen v0.3.6 release.
- [ ] Create a fresh consistent SQLite backup, verify `quick_check`, foreign keys, sidecar absence, and SHA-256, then generate a fresh service plan and exact `24/24` preflight within its validity window.
- [ ] Cut over only backend, frontend, and WeChat; keep Caddy, Qingyang, account-vault, Mihomo, and port `8797` unchanged and invoke rollback if any guarded step fails.
- [ ] Obtain post-cutover `24/24`, public HTTPS smoke `25/25` with clean test-data deletion, and real Chrome desktop/mobile acceptance.
- [ ] Record only non-secret production evidence in `docs/部署记录.md`, publish the deployment-record change, and create the requested non-secret desktop handoff directory for continued development on another device.
