# 开发日志

此日志按里程碑汇总关键提交。单个改动的完整差异以 Git 提交为准。

## 2026-07-29：v0.2.2 候选开发中

当前工作目录是 `codex/project-sync-release`，目标版本为 `v0.2.2`，源码与包元数据已切换到 `0.2.2`。本批次尚未完成版本冻结、完整质量门、GitHub Release 或生产部署；下面的内容是工作树中的实现和增量验证记录，不是最终交付结论。

### 已进入工作树的范围

- 快速记录的入口统一经过同一个导航函数，重新打开时回到语音模式；移动端导航、触摸目标和 Safari 表单字号已调整。
- 客户详情默认为只读，编辑需要显式点击修改；取消会丢弃本地变更，删除使用可访问确认对话框并验证取消、重复提交和错误状态。
- 增加 WebKit 验收脚本、静态服务 CSP/HSTS 等响应头、生产 HTTPS 冒烟清理、SQLite 数据库身份绑定，以及 release manifest 和 CI 复核。
- 正在补充受控 cutover 的项目服务 allowlist、回滚和 PID/Caddy 配置快照。

### 增量验证记录

开发过程中已分别完成视觉节奏 `10/10`、生产清理 `16/16`、数据库身份 `3/3`、HTTPS 冒烟工具 `20/20`、认证 HTTP `13/13` 和发布工具组合 `55/55` 等检查，前端构建、客户集成流程和 WebKit 关键流程也曾通过。

最后一次移动视口调整和集成断言补充后，完整根测试、后端测试和 `qa:full` 尚未重新执行。正式版本不能引用这组增量结果，必须在文档和代码冻结后重新跑完整门禁。

## 2026-07-28 至 2026-07-29：生产上线与版本治理

| 时间 | 内容 |
| --- | --- |
| `2026-07-28T21:19:52+08:00` | `f89e1e7` 合并到 GitHub `main`，完成生产共享 Caddy 适配 |
| `2026-07-28T22:30:21+08:00` | backend/frontend 切换到不可变 release；微信 worker 于下一秒启动 |
| `2026-07-28T23:21:42.497+08:00` | 生产预检 `18/18`，公开 HTTPS 冒烟 `25/25`，数据库完整性和受保护服务复核完成 |
| `2026-07-29T01:19:27.509+08:00` | 冻结 `v0.2.0` 文档、版本治理和发布门禁范围；根测试 `77/77`，后端 `267/267`，前端本地与集成质量门通过 |
| `2026-07-29T01:40:43.097+08:00` | PR #10 首轮 CI 暴露浅克隆与强制历史扫描不兼容；按 TDD 补齐完整历史 checkout，根测试更新为 `78/78` |
| `2026-07-29T02:15:12.059+08:00` | PR #10 第二轮暴露视觉 QA 的 CDP 无限等待和 Linux 清理缺口；按 TDD 增加 2 项生命周期测试，完整 `qa:full` 在 141.3 秒内通过 |
| `2026-07-29T02:36:24.435+08:00` | 独立审查关闭 2 项 P1 和 1 项 P2：增加进程组验证、Windows 失败关闭和 HTTP server 独立清理；视觉 `9/9`，最终 `qa:full` 144.4 秒通过 |
| `2026-07-29T02:43:50+08:00` | PR #10 合并到 `main@591e48d`；PR CI 和合并后的 main CI 均成功 |
| `2026-07-29T02:47:23+08:00` | 创建不可移动的 annotated tag `v0.2.0`，固定指向 `591e48d464341d1df95f541d84790e7452341d5d` |
| `2026-07-29T02:47:59+08:00` | Release run `30389038587` 因 Linux 把 `C:/...` 解析为 `<cwd>/C:/...` 而失败，6 项 WSL 路径测试未通过；没有创建 `v0.2.0` Release 或资产 |
| `2026-07-29T02:57:30+08:00` | `abb677b` 增加跨宿主 Windows/POSIX 路径解析和 POSIX 回归测试；云端 Linux Node 24 复现旧版失败并验证修复后 `13` 项中 `12` 通过、`1` 项按 Windows 平台跳过 |
| `2026-07-29T03:02:34+08:00` | `1c18928` 升级八处正式版本字段到 `0.2.1`，新增热修复说明，并把 PR CI 根门禁对齐为 `node --test scripts/*.test.mjs` |
| `2026-07-29T03:08:13.542+08:00` | `v0.2.1` 候选完整 `qa:full` 在 146.3 秒内通过：全历史扫描零发现、根测试 `80/80`、后端测试与前端本地/集成 QA 全部成功，五类响应式视口横向溢出均为 `0` |
| `2026-07-29T03:16:40+08:00` | `3827321` 关闭独立审查的路径与门禁缺口：拒绝不可可靠映射的 UNC/Windows 根相对工作区，Windows 目标测试 `14/14`、云端 Linux `13` 通过且 `1` 项按平台跳过；共享凭据命令显式固定全历史扫描 |

本批次增加 GitHub 标签发布、不可变归档和 SHA-256 资产，重写项目入口、模块地图、多设备开发、发布回滚和部署记录。生产仍使用独立 Node.js 24，三个项目服务直接固定到真实 release 路径；共享 Caddy 和同机其他系统不随应用发布重启。

`v0.2.0` 标签及失败 workflow 作为不可变事故证据保留，不删除、不移动、不复用。`v0.2.1` 已合并到 `main`、创建 tag 和 GitHub Release，并作为当前生产基线运行；提交、归档摘要和 release 路径见 [部署记录](部署记录.md)。

## 2026-07-15：项目基线与第一阶段安全基础

| 提交 | 内容 |
| --- | --- |
| `da1ff72` | 建立经过验证的项目基线 |
| `61b660c` | 确认 Apple Design 风格一升级设计 |
| `46b895f` | 制定第一阶段安全与数据基础计划 |
| `a491dba` | 隔离并忽略 Codex 工作树 |
| `915395b` | 增加 SQLite 版本化迁移 |
| `66cff5e` | 加固迁移和恢复安全 |
| `a975e59` | 修复迁移审查问题 |
| `2a04dfb` | 加固恢复与并发测试 |
| `820fe0d` | 拒绝损坏的恢复候选 |
| `eca055b` | 增加第一阶段完整性数据结构 |
| `7adcc42` | 验证备份和恢复完整性 |
| `223ed8a` | 增加密码哈希与可撤销会话 |
| `8f96e04` | 强制 Cookie、CSRF 和 Origin 安全 |
| `e62eeab` | 校验全部业务写入请求 |
| `2717be9` | 前端切换 Cookie 会话和受保护导出 |

## 2026-07-16 至 2026-07-17：数据一致性和审计

| 提交 | 内容 |
| --- | --- |
| `5f2548f` | 增加乐观版本与软删除 |
| `9cfc807` | 快速记录确认改为原子且幂等 |
| `f6f5dec` | 记录认证后的原子变更审计 |
| `f35cfcf` | 验证第一阶段安全与恢复 |

## 2026-07-19：发布候选真实性和交互

| 提交 | 内容 |
| --- | --- |
| `a13f3d0` | 制定发布候选加固计划 |
| `11677bf` | 移除前端演示数据回退 |
| `50d89b7` | 强制业务数据只来自后端 |
| `818c709` | 修复业务数据竞态 |
| `c2bf4e9` | 移除静态商机时间线 |
| `f21be49` | 移除演示商机阶段数据 |
| `521e4c8` | 按产品决策暂缓方案辅助写入 |
| `517c2ab` | 增加可移植发布包和生产预检 |
| `b71d85e` | 保证全部商机阶段数量守恒 |
| `5e33a60` | 增加规范化业务路由 |
| `5a8da98` | 加固发布包和预检校验 |
| `0730bd6` | 强制方案辅助暂缓的集成契约 |
| `1323dd4` | 加固规范路由验证 |
| `338fbf1` | 收紧发布所有权和敏感信息门禁 |
| `f63eebf` | 加固路由状态输入 |
| `81f0f61` | 收窄路由位置对象契约 |
| `d4f226c` | 持久化可编辑的快速记录分析历史 |
| `5996336` | 验证分析持久化和周报展开 |
| `563456d` | 关闭执行命令和敏感扫描绕过 |

## 2026-07-27：专项开发记录

### 智能拜访行程

- 新增高德客户端、确定性行程优化器和 SQLite 行程快照；路线顺序优化的模型调用仍按现有行程链路单独管理
- 新增前端高德 Geocoder 坐标提示与后端逆地理城市复核，解决当前 Web 服务正向编码 `30001` 的实际运行阻断
- 完成列表/详情/修改/删除/空白新建、地图折线、移动端时间轴和历史零重算验收
- 后端全量测试 `253/253`、前端全量测试 `156/156`；真实 Chrome 集成覆盖 1440×900、834×1194、390×844、360×800，横向溢出均为 0
- 真实高德 UI 流程创建并删除测试行程通过，路线结果约 379.1 km、4 小时 10 分、146 元

### 复杂销售决策 Agent V1

- 完成《森特智行复杂型 B2B 销售决策 Agent V1》规则规格，包含证据/推断/假设/未知分离、决策链、阶段门槛、评分封顶、行业规则、合规红线、JSON 合同和评估案例。
- 规格文档已通过必需章节、UTF-8、JSON 示例和 `git diff --check` 检查，文件为 `backend/src/ai/agents/sales-decision-agent-v1.md`。
- 架构决定是拆分核心 system prompt、场景 playbook、行业规则包、JSON Schema 和 eval 集，再接入 `backend/src/modelAnalysis.js`；在接入计划和测试完成前，不覆盖现有快速记录分析。
- 当前状态：V1 规则规格已完成，但尚未按该完整规格接入现有 DeepSeek 运行时，不能把它写成已经上线的业务能力。快速记录的 DeepSeek 分析是独立链路。

### 待继续事项

- 页面路由状态整合
- 语音资产数据库、私有存储和恢复
- 微信事件幂等、草稿持久化和单消费者保护
- 销售决策 Agent V1 的 DeepSeek 接入计划、契约测试和真实案例校准
- 发布包与生产预检最终门禁

每项内容只有在提交、完整测试和主控制会话复核后，才加入正式版本日志。
